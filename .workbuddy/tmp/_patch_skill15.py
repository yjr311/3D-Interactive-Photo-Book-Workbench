# -*- coding: utf-8 -*-
"""verify-html-in-browser 技能回改（0.9.0 → 1.0.0）。

第十五轮暴露的是**新的**一类失灵：不是阈值不对、也不是样本分辨不出，
而是「**量错了对象**」—— "连渲 N 次是否同值"量的是**确定性**，
而那个 bug 的症状是「**确定的错值**」：同一份代码里连渲 12 次完全一致，
只是那个值由 <1e-6 的舍入差裁决。检测器全绿，产品是错的。
"""
import io

P = r'C:/Users/zz/.workbuddy/skills/verify-html-in-browser/SKILL.md'
s = io.open(P, encoding='utf-8').read()


def rep(old, new, n=1):
    global s
    assert s.count(old) == n, '锚点出现 %d 次（期望 %d）：%r' % (s.count(old), n, old[:70])
    s = s.replace(old, new, n)


# ---- 版本 ----
rep('version: "0.9.0"', 'version: "1.0.0"')

# ---- 在 §8 之后追加 §9 / §10 ----
NEW_SECTIONS = '''### 9. 判据要对「**全部变体**」成立，不是对你怀疑的那一个

用户报的往往是"某一个具体变体坏了"（"这个*牛皮纸*模版的贴纸拖不动"）。
按直觉去查那一个，通常真能查到根因 —— 但**修完就走，会漏掉同源的其它变体**。
本项目实测：盯着牛皮纸看只能看到 1 个问题，**把 11 个变体各渲一遍打成一张表，
看到的是 8 个**（3 个坐标越界、5 个被溢出撑大、1 个退化成兜底、1 个跳格）。

做法三步，成本很低：

1. **枚举全部变体，把中间量做成一张表**（不是只看截图）：
   ```js
   Object.keys(L.TPL).forEach(function(k){
     var cv=L.renderCanvas(p,k,L.optsFor(p),LE,true);
     out.push({k:k, rc:[rc.dx,rc.dy,rc.dw,rc.dh], bad:(rc.dw<=1||rc.dh<=1||rc.dx<0||rc.dy<0||...)});
   });
   ```
2. **对这张表下一条"近乎废话"的结构性判据**（"矩形必须落在画布内"）。
   废话型判据的价值恰恰在于**覆盖面**：它不需要你提前知道该怀疑谁，
   对全部变体一视同仁。写"我认为应该等于 X"的判据就只能抓到你想到的那一个。
3. **再配一条反面对照**：把**修前**那个值喂给同一条判据，必须判"不合格"。
   不然"全部合格"也可能只是判据抓不住东西。

配套：`字符串硬编码的期望值` 只用于**承重**的判据。若某个值本身是"任意的但必须确定"
（本项目九宫格"面积最大者胜"，9 格面积差在 1e-6 以下，赢家由舍入决定），
就把**实测到的那个值**钉成定值断言，并在注释里写清"它一变动说明有人动了那条判据"。

### 10. 检测器的失灵方式之——「**量错了对象**」

第 5 条讲的是"检测器把产品量慢了"，第 9 条讲的是"判据覆盖面不够"。
还有第三种，最隐蔽：**判据本身算得好好的，但它量的那个属性根本不是出问题的属性。**

真实案例：一个贴纸锚点会"跳到另一格"的 bug。检测器写的是
「同一设置连渲 N 次，矩形必须同值」—— 这条**全绿**。
因为那个 bug 在同一份代码里是**确定的**：连渲 12 次逐位相同。
它真正的问题是**那个确定的值是错的**（面积比较用了严格 `>`，
而 9 格面积差在 1e-6 px² 以下，"比前者大亿分之几"的格子就胜出 —— 赢家由舍入决定）。

**怎么发现自己量错了对象**：把这几个说法分开问自己 ——

| 我在量 | 它在什么情况下会红 |
| --- | --- |
| 确定性（同输入多次结果一致）| 只有真的引入随机/次序依赖才会红 |
| 正确性（结果等于某个应当的值）| 值算错就红 |
| 分辨力（判据能不能抓住已知的坏样本）| 判据失效时红 |

这个 bug 要的是**第三行**。所以通用做法是：

- **让检测器去跑「已知坏掉的产物」。** 造一份残缺产物（见下节），
  把整套/新加的断言都打上去 —— **坏的那个必须被报出来**。
  本项目实测：新增 12 条里有 9 条在"改前"产物上失败（含用户报的那两条本身）。
  这一步不能省，否则"404 项全绿"什么都不能证明。
- 一个属性换一种**量法**，往往就能抓到：这里抓它的是"把 1e-6 容差拿掉、
  看赢家会不会换格子"（`352.6,184` → `623.7,455`），而不是"多渲几次"。

'''

rep('\n## 断言要打到「用户能看见的那一层」\n', '\n' + NEW_SECTIONS + '## 断言要打到「用户能看见的那一层」\n')

# ---- 改写"出改前/改后对照图"那一节：加"改产物"这条快路子 + 隔离实验 ----
OLD_HEAD = '### 出「改前 / 改后」对照图：用改动前的源码**再组一份产物**\n'
NEW_HEAD = '''### 出「改前 / 改后」对照图 + 造「只坏一处」的残缺产物

#### 两条造"改前产物"的路子，优先用第二条

- **回退源码再 build**（下面这段）：最"正统"，但慢，而且要保证构建可复现。
- **直接在**产物**上做字面量替换**：**快得多，且能精确回退到"只差你改的那一行"**。
  适合产物是压缩单文件的情形（本项目：`_before.html` = 在压缩产物上把
  `return rectThruM(d,l)}` 换回 `return c}`）。

后者有个额外好处：**可以造"只坏一处"的隔离实验产物**。
一次改动里同时修了 4 个机制时，一张"改前 vs 改后"的对照**说不清哪条修好了什么**；
造 3 份产物（`_before` 全坏 / `_tolon` 只去容差 / `_nointer` 只去求交）跑同一张表，
就能把每个机制的靶子钉死 —— 本项目实测：牛皮纸只被 CTM 影响，
明信片/霓虹/极简只被"求交"影响，九宫格只被容差影响，双色只被 `declare` 影响，
**四处改动各有各的靶子，一处都没白改**。

替换时必须遵守（压缩产物尤其危险，一行就是几万字符）：

```python
assert out.count(old) == 1, '锚点出现 %d 次，拒绝打补丁' % out.count(old)   # ① 锚点唯一
cand = out.replace(old, new)
assert cand != out and len(cand) < len(out), '替换没生效'                    # ② 真的变了
# ③ 值级校验：新串在、旧串不在、**被匹配区间里要保留的内容还在**
assert 'function renderCanvas' in cand and cand.count('function renderCanvas') == 1
```
⚠ 替换串必须把**匹配区间内所有要保留的内容原样带回去**。本项目踩过一次：
替换串只带了两个捕获组、没把中间那段要保留的内容带回来，
于是 7 套配色的全部字段被静默删除，**文件变短了，看起来还像"补丁成功"**。

#### 拍图这一半

''' + OLD_HEAD
rep(OLD_HEAD, NEW_HEAD)

# ---- 小坑：加两条 ----
rep('### 三个一踩就中的小坑\n', '### 几个一踩就中的小坑\n')
rep('''   ⚠ 改名时按**词边界**替换（`re.sub(r"\\be1\\b", "v1", line)`）—— 否则 `e1u`/`e1wx`
   这类派生名会被拆坏。
''', '''   ⚠ 改名时按**词边界**替换（`re.sub(r"\\be1\\b", "v1", line)`）—— 否则 `e1u`/`e1wx`
   这类派生名会被拆坏。
4. **重新编号不要"就地顺序替换"。** 想把 `100→101 / 101→102 / 102→103` 各挪一格，
   按这个顺序跑一遍会**连锁**：`100→101` 之后，第二步的 `101→102` 会连新生成的 101 一起改，
   最后 100/101/102 全被推到 103（只剩一串重复的 103）。要么**从后往前**，
   要么干脆**把整段按出现顺序重跑一遍**：
   ```python
   n = [91]
   tail = re.sub(r"'TAG-\\d+ ", lambda m: "'TAG-%d " % (n.__setitem__(0, n[0]+1) or n[0]), tail)
   ```
   改完立刻校验：`labels` 数量、`range`、有没有重复、有没有空洞。
5. **CDP 截到的整屏比 `windowSize` 小。** `launch({windowSize:'1400,900'})` 下
   `Page.captureScreenshot` 拿到的是 **1384×805**。两个后果：
   - 拿 `1360×790` 以内的画布做"页内铺满"（按 1400×900 铺，底部会露在截图外）；
   - 拼图时 `PIL.crop()` 的框**必须钳到图内** —— PIL 允许越界，越界部分**补黑**，
     图上会多出一条黑边，看起来像"截图截坏了"。
6. **拖拽用真实鼠标时，`mouseMoved` 要显式带 `buttons: 1`。** CDP 的
   `Input.dispatchMouseEvent` 默认 `buttons: 0`（等于"没按键在按"），
   从 `mousePressed` 到 `mouseReleased` 之间的移动会被页面当成"已经松开了"。
   本项目 `cdp.mjs` 的 `mouse(type,x,y,opts)` 支持 `opts` 覆盖，拖拽时传 `{buttons:1}`；
   并且**分步走**（6 步以上），一步到位不算拖拽。
''')

assert '### 9. 判据要对' in s and '### 10. 检测器的失灵方式' in s
assert 'version: "1.0.0"' in s
assert '1384×805' in s and 'buttons:1' in s
assert '两个捕获组' in s
io.open(P, 'w', encoding='utf-8', newline='').write(s)
print('SKILL.md patched -> %d chars' % len(s))
