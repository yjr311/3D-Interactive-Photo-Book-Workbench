# -*- coding: utf-8 -*-
"""第十五轮记忆补丁：MEMORY.md 加硬约定 51–57、改套件门槛/计数；
   新建当日日志 .workbuddy/memory/2026-09-21.md。"""
import io, os

ROOT = r'C:/Users/zz/WorkBuddy/2026-09-11-11-24-14/'
MEM = ROOT + '.workbuddy/memory/MEMORY.md'
LOG = ROOT + '.workbuddy/memory/2026-09-21.md'

RULES = '''51. **一个值的「坐标系」要在它出生的地方说清楚（第十五轮牛皮纸）。** `paintPhoto` 返回矩形，
    却从没说"在哪个坐标系里" —— 于是它有了两个消费者（`cx.paint` / `renderPlain`，都按**画布像素**理解）
    和一个**含义不同的生产者**：牛皮纸是先 `translate(W/2,H*.45); rotate(-.028)` 再 `c.paint(-pw/2,-ph/2,…)`，
    量出来的矩形是**局部坐标**（实测 `-345.6,-460.8`，960×1280 下 3/4 在画布外、中心压在左上角）。
    面板换算 `clamp((px-rc.dx)/dw,0,1)` → 往右/往下算出来超 1 → 被夹住 → **纹丝不动**，
    只有左上方向能动（用户原话：「插入的贴纸无法往右以及往下拖拽」）。
    修法 `rectThruM(rc, ctx.getTransform())`：四个角过一遍**绘制那一刻的 CTM** 取外接矩形。
    三条纪律：① **CTM 是单位矩阵时原样返回**（绕一圈会带进浮点噪声，而下游有"严格 `>` 挑面积最大者"
    这种把噪声当信号的判据）；② **不返回矩阵**（返回矩阵 = 给"两套坐标系"开后门，见规则 45）；
    ③ 代价是四角最多差 `半宽×sinθ`（牛皮纸 1.6° → 1.6%），但**旋转下矩形中心不变**，
    所以 `(.5,.5)` 仍然精确 —— 那正是最要紧的一格。
52. **要验的不变量必须对「全部变体」成立，不是对你怀疑的那一个。** 静态扫 11 个模版的 `draw()`
    确实抓到了牛皮纸（唯一在 `paint` 前有 `translate/rotate` 的），但**漏了**双色（压根不调 `c.paint`）
    和九宫格（平局）。真正的做法：**让 11 个模版各自渲一遍、把 `photoRect` 打成一张表**
    （`_probe_kraft.mjs`），再对这张表下一条**近乎废话的结构性判据**（"矩形必须落在画布内"）——
    表一打出来谁不对劲自己就跳出来。修前 **8/11** 个模版有病（`kraft`/`polaroid`/`postcard` 越界，
    `neon`/`minimal`/`cinematic` 被 cover 溢出撑大，`duotone` 退化，`grid9` 跳格）。
    **判据自己也要有反面对照**：把修前的值喂给同一条判据，必须判不合格。
53. **"面积最大者胜"这类比较要带容差。** 九宫格画 9 格，9 格的面积**差在 1e-6 px² 以下**，
    严格 `>` 会让"比前者大亿分之几"的格子胜出 —— **赢家由舍入决定，不由设计决定**。
    加 `1e-6` 容差把它钉成"先来先得"（实测：去掉容差赢家从 `352.6,184` 跳到 `623.7,455`）。
    ⚠ **它在一个版本里是确定的**（同代码连渲 12 次同值）—— 所以"连渲 N 次对比"这条检测器**抓不到它**，
    抓它的是"把容差拿掉、看赢家会不会换格"。
    **同一个数"当坐标用"和"当尺子用"，容差要求完全不同**：`drawFit` 的返回值 3 个消费者里，
    `renderPlain` 拿它当坐标（差 0.001px 无所谓）、`makeDuotone` 直接忽略它、
    而 `cx.paint` 拿它**比面积** → 浮点噪声于是变成了产品行为。
54. **cover 溢出的那一圈不是照片。** `paint(x,y,w,h)` 是"铺满这个框、多出来的裁掉"，
    所以**可见照片 = 铺出来的矩形 ∩ 画框**，`paintPhoto` 里必须求交。不求交的后果（实测溢出量）：
    明信片上下各 **158px**、电影感 173、霓虹 136、极简 64、宝丽来 3 ——
    贴纸在这些边上会飘到照片外面，而面板画的虚线还写在"照片边界"上，自相矛盾。
    （`contain` 时矩形本来就小于画框，求交是恒等变换，不会误伤。）
55. **模版不一定走 `c.paint` —— 所以要有一个"申报"的口子。** 双色自己 `makeDuotone()` 重着色之后
    `ctx.drawImage`，于是**没人吭声** → `photoRect` 退化成"整块画布"，贴纸锚在画布中心而不是照片中心
    （**偏 90px**）。补法是给 `cx` 加 `declare(x,y,w,h)`：语法糖而已，语义是"照片就占这个框"，
    不再跑一次 fit 计算 —— 让"我自己贴上去的照片"也能进入同一个坐标系。
56. **同时改多处时，必须做"隔离实验"。** 这一轮同时动了四处（CTM / 与画框求交 / 1e-6 容差 / `declare`），
    一张"改前 vs 改后"的对照说不清哪条修好了什么。造"**只坏一处**"的产物（`_tolon.html` 只去容差、
    `_nointer.html` 只去求交）+ `_before.html`（全去）跑同一张表，才确认：
    牛皮纸只被 CTM 影响，明信片/霓虹/极简/电影感/宝丽来只被"求交"影响，九宫格只被容差影响，
    双色只被 `declare` 影响 —— **四处改动各有各的靶子，一处都没白改**。
    （做法：`.workbuddy/tmp/_mkbefore.py` / `_mkvariants.py` 直接在压缩产物上做字面量替换造残缺产物，
    比回退源码再 build 快得多；替换后必须做**值级校验** + 反面对照。）
57. **新断言必须能判"坏" —— 打回「改前」产物跑一遍。** 把 `test_batch2` 指到 `_before.html`，
    新增的 12 条里有 **9 条失败**（含"往右/往下拖不动"两条本身）。不跑这一步，
    "404 项全绿"什么都不能证明（规则 43 的第四次应验，但这次的失灵方式是新的一种：
    不是阈值不对，是**量错了对象** —— "连渲 N 次"量的是确定性，而这里的病是"**确定的错值**"）。

## 用正则给源码打补丁
'''

# ---- MEMORY.md ----
s = io.open(MEM, encoding='utf-8').read()

old_rule_end = '\n## 用正则给源码打补丁\n'
assert s.count(old_rule_end) == 1
s = s.replace(old_rule_end, '\n' + RULES, 1)

old_thr = '- **10 个套件 / 393 项全绿才算过**（2026-09-20 第十四轮修订）：'
assert s.count(old_thr) == 1
s = s.replace(old_thr, '- **10 个套件 / 404 项全绿才算过**（2026-09-21 第十五轮修订）：', 1)

old_b2 = '''  `test_batch2.mjs` **90**（**第二批「能玩」** + 第十四轮贴纸坐标修复：贴纸数据层 / 烧进成片（清空后逐像素还原）/
  5 画幅归一化落点一致 / 八枚都能画 / 面板真实点击与拖动 / 音效 / 九宫格 / 封面封底寄语 / 真实 CDP 拖拽 /
  **人造三矩形落点 + 纯平移对照 / 三种真模版矩形互异 / 面板画布与成片逐像素相同 /
  "不点生成书页自己变"（含"只重出这一张"与两条反向对照）/ 真实按钮删除也立刻生效**）。'''
assert s.count(old_b2) == 1
s = s.replace(old_b2, '''  `test_batch2.mjs` **101**（**第二批「能玩」** + 第十四轮贴纸坐标修复 + 第十五轮照片矩形修复：
  贴纸数据层 / 烧进成片（清空后逐像素还原）/ 5 画幅归一化落点一致 / 八枚都能画 / 面板真实点击与拖动 /
  音效 / 九宫格 / 封面封底寄语 / 真实 CDP 拖拽 / 人造三矩形落点 + 纯平移对照 / 三种真模版矩形互异 /
  面板画布与成片逐像素相同 / "不点生成书页自己变"（含"只重出这一张"与两条反向对照）/ 真实按钮删除也立刻生效 /
  **（第十五轮）11 个模版 `photoRect` 必须落在画布内（配反面对照）/ 连渲 3 次逐位相同 /
  双色不许退化成整块画布 / 九宫格锚点定值 / "改前 8 个有毛病的模版一个都不复现" /
  牛皮纸真实鼠标四向拖拽（往右/往下单列一条）/ 面板上那枚贴纸真的画在画布上**）。''', 1)

# 出图那一段加一条截图口径（这一轮真实踩到）
old_shot = '  ⚠ 给 `test_batch2` 插新块时**临时变量名要避开后面已用的**'
assert s.count(old_shot) == 1
s = s.replace(old_shot,
  '  ⚠ **重新编号要从后往前、或直接整段按出现顺序重跑一遍**：先 `100→101`、再 `101→102` 会连锁\n'
  '  （100 一路被推到 103，最后 100/101/102 全空、只剩重复的 103）。\n'
  '  ⚠ **CDP 截到的整屏是 `1384×805`，比 `windowSize:1400,900` 小**；按 1400×900 去 `PIL.crop()`\n'
  '  越界部分会**补黑**（图上多一条黑边，看着像截图截坏了）。拼图脚本里 crop 必须**钳到图内**；\n'
  '  页内铺覆盖层时画布也按 `1360×790` 做，别按 windowSize。\n'
  '  ⚠ 给 `test_batch2` 插新块时**临时变量名要避开后面已用的**', 1)

assert s.count('51. **一个值的「坐标系」') == 1
assert '**10 个套件 / 404 项全绿才算过**' in s
assert '`test_batch2.mjs` **101**' in s
assert '1384×805' in s
io.open(MEM, 'w', encoding='utf-8', newline='').write(s)
print('MEMORY.md  %d chars' % len(s))

# ---- 当日日志 ----
LOG_TEXT = '''# 2026-09-21 · 第十五轮：牛皮纸「贴纸只能往左上拖」

## 用户报的
> 「有个bug这个牛皮纸，插入的贴纸无法往右以及往下拖拽」

## 根因（实测，不是推测）
`photoRect` 这个中间量**串了坐标系**。牛皮纸模版是先 `translate(W/2,H*.45); rotate(-.028)`
再 `c.paint(-pw/2,-ph/2,…)`，`drawFit` 量出来的是**调用点的局部坐标**：`-345.6,-460.8`
（960×1280 下 3/4 在画布外、中心压在画布左上角）。它被当成**画布像素**用 →
面板 `clamp((px-rc.dx)/dw,0,1)` 往右/往下算出来超 1 → 被夹住 → 纹丝不动。
贴纸 `(.5,.5)` 被画在画布 `x=-1.1`（画布左边缘外面）。

## 改了什么（core.js，四处）
1. 新增 `rectThruM(rc, m)`：四角过 CTM 取外接矩形；**单位矩阵原样返回**；**不返回矩阵**。
2. `paintPhoto`：先与**画框**求交（cover 溢出的不算照片），再过 CTM。
3. `cx.paint` / `cx.declare` 的"面积最大者胜"加 `1e-6` 容差；新增 `declare(x,y,w,h)`。
4. `duotone.draw()` 里 `c.declare(m,H*.1,W-m*2,H*.66)` —— 它不调 `c.paint`，必须申报。

实测矩形（改前 → 改后）：
`kraft -345.6,-460.8,691.2,921.6 → 123.8,182.5,712.4,787.1`；
`postcard 52.8,-158.4,854.4,1139.2 → 52.8,52.8,854.4,716.8`；
`neon 101.8,20.5 → 101.8,156.2`；`minimal 192,128,576,768 → 192,192,576,640`；
`cinematic 0,0,960,1280 → 0,172.8,960,934.4`；`polaroid 72,-2.9 → 72,87`；
`duotone 0,0,960,1280 → 96,128,768,844.8`；`grid9 255.8,21.8 → 352.6,184`。
牛皮纸四向真实鼠标拖拽：瞄 `0.85/0.5`→`0.849/0.5`、`0.5/0.85`→`0.503/0.849`（改前这两条**一动不动**）。

## 一个必须纠正的旧记忆
我原先记的是"九宫格三格面积**按位完全相等**，严格 `>` 让浮点噪声裁决平局"。
实测 **连渲 12 次同值** —— 它是**确定的**。真机制是面积**差在 1e-6 以下**，
"比前者大亿分之几"就胜出，赢家由**舍入**决定。所以"连渲 N 次对比"抓不到它。
隔离实验（`_tolon` 只去容差 / `_nointer` 只去求交 / `_before` 全去）才把四个改动各自的靶子分清。

## 产物与验证
- 产物 `咔哒书-3D互动照片书.html` **1,176,576 B（js 段 1109.8 KB / 余量 79.8 KB）**。
- `test_batch2` 90 → **101**；`_gate.py` 全量 **404 项全绿**（`test_mat` / `test_batch2`
  各触发一次"没起来"重试，重跑即全绿）。
- **新断言打回 `_before.html`：9 项失败**（含往右/往下两条本身）。
- 新脚本：`_probe_kraft.mjs`（11 模版矩形表）、`_cmp_kraftdrag.mjs`（改前/改后四向拖拽）、
  `_cmp_rectdet.mjs`、`_iso_rect.mjs`（四产物隔离实验）、`_mkbefore.py` / `_mkvariants.py`（造残缺产物）、
  `_shot_kraft15.mjs` + `_compose_kraft15.py`（取证图）。
- 取证图：工作区 `咔哒书-贴纸坐标系-改前改后.png`（素材在 `.workbuddy/tmp/shotsb15/`）。

## 文档
- `咔哒书-设计方案.md`：新增 **§4.13.7**（CTM 根因）、**§6.8**（"坐标系要在出生地说清楚"）、
  §六 总计数 → 第十五轮 404、`test_batch2` 表格行 → 101/101、§7 交付行加第十五轮。
- `咔哒书-优化升级方案.md`：新增 **§9.1.10**、§9 表格行、§9.1.8 的口径说明（393 → 404）、§9.1 导读。
- `MEMORY.md`：新增硬约定 **51–57**，套件门槛 393 → 404，`test_batch2` 90 → 101，
  出图段加两条（重编号连锁、截图 1384×805 / crop 越界补黑）。
'''
io.open(LOG, 'w', encoding='utf-8', newline='').write(LOG_TEXT)
print('log written  %d chars' % len(LOG_TEXT))
