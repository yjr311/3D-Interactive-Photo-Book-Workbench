# -*- coding: utf-8 -*-
"""第十二轮文档收尾：图标重做（手绘小书）落地 + 计数口径校正。

每一处都是"整段字面量 → 整段字面量"的替换，并断言命中次数。
不要用 [\s\S]*? 跨区间匹配 —— 上一次就是这么把 7 套皮肤的配色字段静默删掉的。
"""
import io, sys

DOC = r"C:/Users/zz/WorkBuddy/2026-09-11-11-24-14/咔哒书-设计方案.md"
README = r"C:/Users/zz/WorkBuddy/2026-09-11-11-24-14/README.md"

NEW_412 = '''### 4.12 图标重做：手绘小书（第十二轮）

**用户反馈**：「logo不是很好看呀，有没有类似这种的」，并附了一张 **16×16 的手绘小鱼**截图。
**那是风格参考，不是要一条鱼** —— 要的是"手绘、暖色、彩铅"的质感，以及它这种小尺寸下的读法。

#### 4.12.1 没有凭感觉画，先把参考图量了一遍

16px 的位图小到看不清"手感"，但它的**数字**是确定的。逐像素打亮度矩阵，反推出三件事：

| 量出来的 | 参考图的值 | 第一版我画的 | 结论 |
| --- | --- | --- | --- |
| 笔宽 / 边长 | **7.5%** | 11.2% | 粗了 50%，所以"糊" |
| 描边亮度 | **60**（近黑） | 60 | 一致 |
| 填充亮度 | **176**（中浅） | 223 | 亮了 47，所以"飘" |
| 填充 / 描边 对比 | **2.9 : 1** | 3.7 : 1 | 对比过强，像贴纸不像手绘 |
| 图内细节数 | **2**（一只眼睛 + 一条鳍） | 5 | 细节一多，16px 就成一团 |

第一版"看着不对劲"的原因全是**算术**，不是审美。改数值，不改灵感。

#### 4.12.2 定稿几何与配色

48 网格，一本书（左页 + 右页 + 中缝 + 两条页线）：

1. 绕 `(24,24)` 缩放 `sx=1.055 / sy=1.17` —— 把视觉分量补回旧方块外框 `41.6` 的体量，
   不这么做，换上去会显得比原图标"小一圈"；
2. `<g transform="translate(0 1.05)">` 重新居中（缩放后重心上移了）；
3. `<g transform="rotate(-13 24 24)">`，倾斜 13° —— 有"手放上去歪了一点"的活气。

配色：填充 `#eec9a4 → #d7a578` 渐变（暖棕，与参考图同色），描边与页线 `#4a2708`。

> **图标不跟强调色走，是刻意的。** 换氛围皮肤时 `--accent` 会变，但图标固定是暖棕 ——
> 品牌色如果是"当前皮肤色"，它就不成其为品牌色了。反面教材正是被换掉的那一版：
> 它 `fill="currentColor"`，跟着琥珀金走，一到浅色主题/换皮肤就变成另一种颜色。

顶栏投影同时从 `0 5px 15px var(--glow)`（琥珀光晕）换成 `0 2px 5px rgba(0,0,0,.34)` ——
光晕配棕色实物感有点冲，实物贴纸应该配中性投影。favicon 换成同一份矢量
（`data:image/svg+xml;base64` 内嵌，**仍然不引外部文件**，单文件离线这条没破）。

#### 4.12.3 踩到的三个坑

1. **别在"预览页"里看图标。** 一开始把图标贴进一个单独的小页面预览，好图标看着像废的 ——
   因为真产物里 `.brand .logo` 的 29px 是靠 `.brand` 包裹层的 CSS 给的，
   少了那层包裹，SVG 就按 `viewBox` 原尺寸（几十像素）铺开、周围一圈空。
   **要预览就把它贴进真产物**：正则在产物副本里替换那一段 SVG，再截图。
   （凡是"CSS 给的尺寸"，脱离父级看都会失真。）
2. **内联 SVG 的 `id` 必须逐份唯一。** `linearGradient id="kadaLogoG"` 在顶栏和方案页里
   被复制了多份，同一个 id 出现两次时浏览器只认第一份 —— 改一份颜色，另一份跟着变，
   排查方向会完全跑偏。
3. **16px 的位图只能逐像素量，不能靠放大截图看。** 放大插值会把 1px 的抖动渲染成
   "设计感"，照着截图描会一路跑偏。

#### 4.12.4 验收

`test_book.mjs` 新增 B40–B46 七项：图标 29×29 / 新路径在、旧方块路径**必须不在** /
渐变端点正好是 `#eec9a4,#d7a578` / **把图标栅格化后数像素**（有墨、且是暖色、且旧方块色为 0；
纸色同理）/ favicon 与顶栏同源 + `xmlns` 在 + 2 个 stop / favicon 16px 栅格化后描边与填充都在。

其中"数像素"那两项**自带反向对照**：同一段代码去数旧方块，必须数出 `0`。
不这么做，"图标换了"就只是一句声明。

对照图：工作区 `咔哒书-图标对照-落地后.png`（改前/改后 × 深色/浅色，3× 实拍，取景完全相同）。
'''

EDITS_DOC = [
    # ① §4.10 挂上"图标已被取代"的牌子
    ('**图标**：沿用原来的形状资产（**圆角方块 + 琥珀金**），把里面的「匣」字换成一个真正的图形 ——\n**一本摊开的书，从方块里挖空出来**。',
     '> ⚠ **这一版的图标（圆角方块 + 挖空的书）在第十二轮被换掉了** —— 用户原话「logo不是很好看呀」。\n'
     '> 现状见 **§4.12**（手绘小书）。本节里**品牌名字**、改名迁移、以及下面那两个坑（绕原点缩放 / mask）\n'
     '> 都仍然有效，受影响的只有图标本身。\n\n'
     '**图标（已废弃）**：沿用原来的形状资产（**圆角方块 + 琥珀金**），把里面的「匣」字换成一个真正的图形 ——\n'
     '**一本摊开的书，从方块里挖空出来**。'),

    # ② 新增 §4.12（插在 §4.11 与「## 五」之间）
    ('\n---\n\n## 五、界面设计（创意点）\n', '\n' + NEW_412 + '\n---\n\n## 五、界面设计（创意点）\n'),

    # ③ test_book 行：44 → 51，补图标覆盖
    ('| `test_book.mjs` | 书本观感 + 翻页手感 + 书页画面（含成片新鲜度 / 改模版判过期）/ **改名迁移（旧默认值换新名、用户自己改过的不动、opts.title 不许被碰）** | 44/44 |',
     '| `test_book.mjs` | 书本观感 + 翻页手感 + 书页画面（含成片新鲜度 / 改模版判过期）/ **改名迁移（旧默认值换新名、用户自己改过的不动、opts.title 不许被碰）** / **图标：29px、新几何在旧几何不在、渐变端点、栅格化数像素（含反向对照）、favicon 同源与 16px 栅格化** | 51/51 |'),

    # ④ test_landing 行：把一直写错的 15/15 改成实测的 12/12
    ('**悬停折角落点（两侧都在书口边，含反向对照）** | 15/15 |',
     '**悬停折角落点（两侧都在书口边，含反向对照）** | 12/12 |'),

    # ⑤ 合计行
    ('**第十一轮合计 299 项检查全绿**（9 个套件；第十轮 290 → 本轮新增"发布尺寸真的落到文件里"9 项）。',
     '**第十二轮合计 303 项检查全绿**（9 个套件：`test_book` 51 / `test_flow` 21 / `test_landing` 12 /\n'
     '`test_real` 27 / `test_skin` 33 / `test_note` 31 / `test_export` 30 / `test_auto` 48 / `test_mat` 50）。\n'
     '本轮为图标重做新增 7 项（`test_book` 44 → 51）。\n\n'
     '> **顺带校正一个口径错误**：之前的文档里 `test_landing` 一直写着 `15/15`，实测一直是 **12**\n'
     '（其中「落地亮度连续」一条在 3 个翻页场景上各跑一次，共 3 项）。\n'
     '> 历史文档的合计（272 / 290 / 299）也因此各自偏大 3。以后**项数以套件自己打印的 PASS 行数为准**，'
     '不要从文档里抄。\n'
     '> 计数脚本：`.workbuddy/tmp/_gate.py`（顺序跑 9 个套件并汇总，避免并行抢 CDP 端口）。'),
]

EDITS_README = [
    ('# 光匣 LUMEN · 3D 互动照片书 —— 设计方案\n\n> 交付物：`光匣-3D互动照片书.html`（单文件，双击即用，零依赖、可离线运行）',
     '# 咔哒书 KADA · 3D 互动照片书\n\n> 交付物：`咔哒书-3D互动照片书.html`（单文件，双击即用，零依赖、可离线运行）\n'
     '>\n'
     '> **这份 README 停在 2026-09-17（改名之前），只覆盖到「零 ~ 七」的前七轮内容，当前权威文档是\n'
     '> `咔哒书-设计方案.md`** —— 品牌改名（第十轮）、发布尺寸（第十一轮）、图标重做（第十二轮）都在那份里。\n'
     '> 两处口径不一致时，以 `咔哒书-设计方案.md` 为准。'),
]


def patch(path, edits, label):
    s = io.open(path, encoding='utf-8').read()
    before = len(s)
    for old, new in edits:
        n = s.count(old)
        assert n == 1, '%s 锚点命中 %d 次（期望 1）：%s' % (label, n, old[:48])
        s = s.replace(old, new)
    io.open(path, 'w', encoding='utf-8', newline='\n').write(s)
    print('%-10s %d → %d 字符' % (label, before, len(s)))


def main():
    patch(DOC, EDITS_DOC, '设计方案')
    patch(README, EDITS_README, 'README')
    return 0


if __name__ == '__main__':
    sys.exit(main())
