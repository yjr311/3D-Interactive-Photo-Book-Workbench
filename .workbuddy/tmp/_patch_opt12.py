# -*- coding: utf-8 -*-
"""优化升级方案：补 §9.1.7（图标重做）+ 校正合计口径（每处都断言唯一命中）。"""
import io, sys

P = r"C:/Users/zz/WorkBuddy/2026-09-11-11-24-14/咔哒书-优化升级方案.md"

NEW_917 = '''#### 9.1.7 补做：图标重做 —— 手绘小书（第十二轮）

| 项 | 内容 |
| --- | --- |
| 用户反馈 | 「logo不是很好看呀，有没有类似这种的」+ 一张 **16×16 的手绘小鱼**截图。**那是风格参考，不是要一条鱼** |
| 先做的事 | 把 16px 参考图**逐像素量了一遍**（亮度矩阵），反推出笔宽/填充亮度/对比度/细节数。第一版"看着不对劲"全是**算术问题**：笔宽粗了 50%、填充亮了 47。改数值，不改灵感 |
| 定稿 | 48 网格的一本摊开的书。绕 `(24,24)` 缩放 `sx 1.055 / sy 1.17` 补回旧方块的视觉分量 → `translate(0 1.05)` 居中 → **`rotate(-13 24 24)`**。填充暖棕渐变 `#eec9a4→#d7a578`，描边与页线 `#4a2708` |
| 关键决定 | 图标**不跟强调色走**。被换掉的那版 `fill="currentColor"`，换氛围皮肤/切浅色主题时整枚图标跟着变色 —— **品牌色如果是"当前皮肤色"，它就不成其为品牌色了**。现在它固定暖棕，像一枚贴在界面上的实物贴纸 |
| 顺带 | 顶栏投影从 `0 5px 15px var(--glow)`（琥珀光晕）换成 `0 2px 5px rgba(0,0,0,.34)`：光晕配棕色实物感有点冲；favicon 换成同一份矢量（内嵌 data-URI，**仍是单文件离线**） |
| 踩的坑① | **别在"预览页"里看图标**：真产物里 `.brand .logo` 的 29px 是 `.brand` 包裹层的 CSS 给的，少了那层包裹，SVG 会按 viewBox 原尺寸铺开、四周一圈空，好图标看着像废的。**要预览就贴进真产物**（正则在产物副本里替换那一段） |
| 踩的坑② | **内联 SVG 的 `id` 必须逐份唯一**：`linearGradient id="kadaLogoG"` 被复制多份时浏览器只认第一份，改一份、另一份跟着变，排查方向会跑偏 |
| 踩的坑③ | 16px 位图**只能逐像素量，不能靠放大截图看** —— 插值会把 1px 抖动渲染成"设计感" |
| 新断言 | `test_book` 新增 7 项：29×29 / 新路径在且**旧方块路径必须不在** / 渐变端点 / **栅格化后数像素**（有墨、是暖色、旧方块色为 0；纸色同理）/ favicon 与顶栏同源 + `xmlns` + 2 stop / favicon 16px 栅格化描边与填充都在。数像素那两项**自带反向对照**：同一段代码去数旧方块必须数出 0 |

**本轮验证**：9 个测试套件 / **303 项检查全绿**（`test_book` 44 → **51**）。
取证图：工作区 `咔哒书-图标对照-落地后.png`（改前/改后 × 深色/浅色，3× 实拍，**四张取景完全相同**）、
`咔哒书-LOGO方案.html`（唯一一份方案页，第十轮那份方块图标的已归档）。

**通用教训**：
1. 有人说"照着这张图做"时，**先把参考图量出来**（它多大？笔多粗？对比多强？细节几个？）。
   凭感觉画的版本会被否，而且否掉之后你不知道该往哪边调 —— 量过之后"粗了 50%"是个可执行的数字。
2. **对照图的公平性来自"同一取景"**，不是事后对齐。改前改后必须同窗口、同缩放、同裁切框，
   否则读者的注意力全在"这两张怎么不一样大"上。

'''

EDITS = [
    # ① §9.1.5 图标行：挂上"已被换掉"
    ('| 图标 | 沿用原来的形状资产（**圆角方块 + 琥珀金**），把里面的「匣」字换成一个真正的图形：**一本摊开的书，从方块里挖空出来** |',
     '| 图标（**第十二轮已更换**） | 沿用原来的形状资产（**圆角方块 + 琥珀金**），把里面的「匣」字换成一个真正的图形：**一本摊开的书，从方块里挖空出来**。⚠ 这一版**被用户否掉了**（原话「logo不是很好看呀」），现状见 §9.1.7（手绘小书）；名字与迁移逻辑不受影响 |'),

    # ② §9.1.5 合计 290 → 287（从第十二轮实测值往回推）
    ('**本轮验证**：9 个测试套件 / **290 项检查全绿**（`test_book` 40 → **44**：新增改名迁移 4 项，其余计数不变）。',
     '**本轮验证**：9 个测试套件 / **287 项检查全绿**（`test_book` 40 → **44**：新增改名迁移 4 项，其余计数不变）。\n'
     '> 287 是从第十二轮的实测值往回推的：303（第十二轮）→ 296（第十一轮 +9）→ 287（本轮 +4）。\n'
     '> 当时文档写的是 290 —— 比实测多 3，因为 `test_landing` 一直被当成 15 项，实际一直是 12 项。'),

    # ③ §9.1.6 合计 299 → 296
    ('**本轮验证**：9 个测试套件 / **299 项检查全绿**（`test_export` 21 → **30**）。取证图：工作区 `咔哒书-发布尺寸-修复对照.png`。',
     '**本轮验证**：9 个测试套件 / **296 项检查全绿**（`test_export` 21 → **30**）。取证图：工作区 `咔哒书-发布尺寸-修复对照.png`。'),

    # ④ 插入 §9.1.7
    ('---\n\n## 10. 我不建议做的五件事', NEW_917 + '---\n\n## 10. 我不建议做的五件事'),
]


def main():
    s = io.open(P, encoding='utf-8').read()
    before = len(s)
    for old, new in EDITS:
        n = s.count(old)
        assert n == 1, '锚点命中 %d 次（期望 1）：%s' % (n, old[:48])
        s = s.replace(old, new)
    io.open(P, 'w', encoding='utf-8', newline='\n').write(s)
    print('ok', before, '→', len(s))

    # 值级校验（只看文件大小/命中数是不够的）
    t = io.open(P, encoding='utf-8').read()
    for w in ['#### 9.1.7', '287 项检查全绿', '296 项检查全绿', '303 项检查全绿',
              '第十二轮已更换', '290 项', '299 项']:
        print('  ', repr(w), '→', t.count(w))
    return 0


if __name__ == '__main__':
    sys.exit(main())
