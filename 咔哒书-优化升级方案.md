# 咔哒书 KADA · 优化升级方案

> **版本** v1 提案 · 2026-09-18  
> **前提** 本文只出方案、不动代码。文中「现状」均已在当前构建（2.99 MB 单文件）里核实。  
> **目标** 让整体体验再上一个台阶，并让它对「小女生群体」有真正的吸引力。

---

## 0. 结论先行

### 0.1 主张：不要改掉现在的高级感，而是给它加一层**可切换的氛围层**

现在的 UI 走的是「暗房 / 摄影集」路子 —— 米白纸、衬线字、克制的留白、克制的动效。  
这是这个产品**唯一不像廉价拼图 App 的地方**。如果为了"少女向"把底色换成粉底 + 贴纸，  
最可能的结果是**两边都不要**：认真相册的人觉得变土了，目标用户觉得"又是一个套壳拼图"。

正确做法是**同一个骨架，两种气质**：

- 默认保持现在的高级感；
- 面板里给一个「氛围」开关（奶油日记 / 蜜桃气泡 / 雾紫梦境 / 薄荷苏打 / 夜樱 / 法式午后），  
  一键把**纸色、墨色、强调色、字体对、颗粒、圆角、模版色板**整套切过去。

三个直接好处：

1. 老用户和"我想认真做本相册"的人不受伤；
2. 新用户的第一屏从「选模版」变成「选氛围」—— **决策成本低一个数量级**；
3. 「我把相册调成蜜桃色了」本身就是可分享的内容。

### 0.2 不建议

**不建议**把"少女向"做成一层贴上去的粉红滤镜，也不建议用 emoji 堆砌来假装可爱  
（详见 §5 的纪律清单）。可爱来自**比例、圆角、留白和字重**，不来自颜色饱和度。

### 0.3 需要你拍板的三件事（其余都能直接做）

1. **手机端算不算这一轮的范围？** 这是本方案里最大的风险项，见 §6。
2. **第一批只做「记录型 + 表达型」两个动机，可以吗？** 见 §1。
3. **视频导出要哪一档？** webm（简单、体积小、部分机型不支持）/ GIF（兼容最好、体积大、要自带编码器）。见 §4.6。

---

## 1. 先把「小女生群体」拆成能指导做功能的动机

笼统的人群画像没法落地。我按**她在什么场景下会打开这个工具**拆成四类，  
每类的诉求完全不同：

| 动机      | 典型场景            | 她最在意                      | 现在缺什么           |
| ------- | --------------- | ------------------------- | --------------- |
| **记录型** | plog、每天拍一张、心情日记 | **快** —— 最好 30 秒出片；竖版能直接发 | 竖版导出、一句话文案、心情标签 |
| **关系型** | 闺蜜本、恋爱纪念日、家庭相册  | **两个人**、日期戳、专属感           | 双人协作、纪念日戳、小卡页   |
| **收藏型** | 追星小卡、演唱会票根、旅行纪念 | **一页多张**、整齐、能打印           | 九宫格小卡页、票根/票券贴纸  |
| **表达型** | 氛围感、情绪、审美展示     | 滤镜、字体、贴纸、音乐               | 氛围皮肤、文案库、贴纸、音效  |

**建议第一批只服务「记录型 + 表达型」。** 理由：这两类是小红书上的**主动发布者**，  
传播效率最高；而关系型和收藏型都要引入"第二个人"或"打印链路"，闭环长得多。

---

## 2. 升级主轴：三层结构

（见聊天里的结构图）

| 层           | 内容                                  | 处置                       |
| ----------- | ----------------------------------- | ------------------------ |
| **底色层**     | 排版引擎、剥离式卷曲翻页、书本结构、持久化、11 套模版        | **不动。这是资产。**             |
| **氛围层**（新增） | 一套 token → 纸色/墨色/强调色/字体对/颗粒/圆角/模版色板 | 所有模版从 token 取色，**不复制模版** |
| **装饰层**（新增） | 贴纸、手写涂鸦、和纸胶带、日期戳、票根                 | 挂在照片上，**跟着照片走**          |

**装饰层能"跟着照片走"这件事，正好接上已经做好的两套机制**：

- `PERSIST` 的自然键（照片名）—— 贴纸直接按照片名存，重开就接回来；
- `bookStale()` —— 贴纸改了要提示"成片已过期"，否则用户又以为没生效。

这两处是现成的，所以装饰层的成本比看起来低。

---

## 3. 功能清单

### 3.1 P0 —— 做出「她会主动截图分享」的最小闭环

| # | 功能                                | 为什么是 P0                                     | 成本 | 风险          |
| - | --------------------------------- | ------------------------------------------- | -- | ----------- |
| 1 | **竖版 + 分享尺寸**（9:16、1080×1440、2:3） | 现在默认 3:4 是给书本的；她要的是能直接发小红书的竖图               | 小  | 低           |
| 2 | **氛围皮肤 6 套 + 「一键氛围成书」**           | 把第一屏的决策从"选模版"变成"选氛围"，懒人 30 秒出片              | 中  | 低           |
| 3 | **文案库 + 心情标签**                    | 她缺的常常不是照片，是"配什么字"；且能直接喂给刚做好的 `{name}` 占位符机制 | 小  | 低           |
| 4 | **贴纸 / 手写涂鸦**（拖拽到照片上）             | 手账感的核心。没有这个，"氛围"只是换皮                        | 中大 | 中（数据模型要想清楚） |
| 5 | **翻页动画导出视频**                      | **传播核武器** —— 一本会自己翻页的相册，比 9 张静态图强太多         | 中大 | 中高（见 §4.6）  |
| 6 | **竖版分享长图**（现有「书页预览长图」升级）          | 一张图讲完整本书，最省事的分发物                            | 小  | 低           |

> ✅ **第 1 / 2 / 3 / 6 项已落地**（氛围皮肤 7 套 · 一键氛围成书 · 文案库与心情标签 · 竖版导出与分享长图），实现细节与踩过的坑见 **§9.1**。
> 第 4 项（贴纸）与第 5 项（视频导出）按原计划留到第二 / 第三批 —— 理由不变：先在分享链路上拿到真实反馈。

### 3.2 P1 —— 让它「能玩」

| #  | 功能                                    | 价值                                     |
| -- | ------------------------------------- | -------------------------------------- |
| 7  | **氛围音效包**（翻页沙沙声已有，扩展为：翻页/落页/贴纸"啵"、可关） | 现成 WebAudio 框架，边际成本极低，感知强              |
| 8  | **九宫格小卡页**（一页 9 张，等分裁切）               | 服务收藏型；顺带覆盖"小卡打印"需求                     |
| 9  | **日期戳 / 纪念日戳 / 票根贴纸**                 | 关系型与收藏型的关键道具，本质是贴纸的一类                  |
| 10 | **「我们一起做一本」双人本**（两人各自导入，各出一半照片）       | 关系型的杀手锏，但需要生成一个可传递的"一半"文件              |
| 11 | **AI 一键美化**（人像提亮、背景虚化、去杂物）            | 显著提升出片率的"魔法按钮"。（注：本项目环境已有图像处理能力，可评估复用） |
| 12 | **双封面 / 封底寄语**                        | 送人的刚需，成本极低                             |

### 3.3 P2 —— 让它「留得住」

| #  | 功能                             | 价值               |
| -- | ------------------------------ | ---------------- |
| 13 | 徽章 / 连续记录（"你已经在咔哒书里存了 100 个瞬间"） | 收集感，但容易做得很土，谨慎   |
| 14 | 模版 & 氛围搭配分享（导出/导入一份"配方"文件）     | 天然的社区传播种子，不需要服务端 |
| 15 | 冲印 / 打印对接                      | 变现路径，但依赖外部供应链    |

---

## 4. P0 逐项实现要点（含坑）

### 4.1 氛围皮肤：一套 token，别复制模版

```
skin = {
  id: 'peach',  name: '蜜桃气泡',
  paper: '#fff6f1',  ink: '#4a3b38',
  accent: '#ff8f7a', accent2: '#ffd9c9',
  serif: '…', sans: '…', hand: '…',       // 字体对
  grain: .05,                              // 颗粒强度
  radius: 14,                              // 圆角基准
  paperTone: 'warm'                        // 供模版派生
}
```

关键点：**模版从 `skin` 派生出自己的色板，而不是给 11 套模版 × 6 套皮肤写 66 份配置。**  
现有 `TPL` 里已有 `paper / ink / accent` 这类可覆盖字段（`opts.paper` 等），  
把这个覆盖源从"用户手填"改成"由 skin 的 token 派生 + 用户可微调"即可。

**坑**：`state.book.paper / ink / cover` 与 `state.opts.paper / ink / accent` 现在是**各存一份**的。  
切皮肤时必须一次性更新两处，否则会出现"书页变粉了但模版还是米色"的割裂。  
建议引入 `applySkin(id)` 作为唯一的写入点。

### 4.2 一键氛围成书

一个按钮，做四件事：**挑氛围 → 挑模版分配 → 挑封面主图 → 直接生成**。

- 模版分配：按 skin 配一组"推荐模版序列"（比如蜜桃气泡 → 宝丽来 / 明信片 / 九宫格），  
  按顺序铺给入册照片；
- 封面主图：复用已有的 `coverScore()` 自动挑图；
- 结果要**立刻可改**（生成前给一个"换一版"按钮，重新随一次），  
  否则"一键"会变成"一次性、不可控"，反而不如手动。

### 4.3 文案库 + 心情标签（直接接到占位符机制上）

上一轮刚做完 `{name} {n} {total} {tpl}` 占位符，**这套机制就是为文案库准备的**。  
新增 `{mood}`（心情标签）、`{date}`（拍摄/今天日期）、`{days}`（距纪念日天数）。  
文案库按情绪分组（治愈 / 元气 / 甜 / 文艺 / 搞怪），每条都是一段可带占位符的短句。

**坑**：文案库的短句**必须能塞进模版的标题行而不溢出**。  
现有模版用的是 `ctx.fillText`，没有自动缩排。要么给文案标注长度上限，  
要么抽一个 `fitFont()`（项目里已有类似函数）统一处理——建议后者。

### 4.4 贴纸 / 手写涂鸦：数据结构要先想清楚

```js
// 挂在照片对象上（跟着照片走，不跟着页面走）
photo.stickers = [
  { k:'tape',   x:.32, y:.14, rot:-.12, s:1.0 },   // 坐标一律归一化 0..1
  { k:'doodle', x:.70, y:.62, rot: .08, s:1.4, text:'好喜欢这天' },
  { k:'stamp',  x:.18, y:.86, rot: .04, s:1.0, text:'2026.09.18' }
]
```

四条设计决定：

1. **归一化坐标**（0..1）而不是像素 —— 缩略图 / 预览 / 成片 / 书页四种尺寸共用一份数据。
2. **贴纸要烧进 `art`**（也就是进成片），因为"贴纸改了"必须能触发 `bookStale()`。  
   做法：在 `renderCanvas` 的 `t.draw(cx)` 之后追加一次 `drawStickers(ctx, stickers, W, H)`。
3. **持久化按照片名存**（`assign[name].stickers`），和模版分配同一个自然键，  
   沿用已有的 `_restored` 标记。贴纸是小数组，体积可忽略。
4. **手写涂鸦走"预置笔迹 + 用户文字"**，不要做自由画笔 ——  
   自由画笔在 canvas 上要处理压感、平滑、擦除，成本翻三倍，  
   而实际产出（歪歪扭扭的字）远不如预置笔迹好看。

### 4.5 竖版导出与分享长图

- `RATIOS` 里已有 `9:16`、`2:3`。要补的是**导出预设**：  
  `小红书竖图 1080×1440`、`故事 1080×1920`、`方图 1080×1080`。
- **分享长图**（现有「导出书页预览长图」的升级）：  
  竖版、每页 1080 宽、页间距固定、顶部带书名与日期，  
  底部留一条空白区（微信/小红书的裁切安全区）。**这是最省事的分发物**，成本极低。

### 4.6 翻页动画导出视频：三种方案与取舍

| 方案                                              | 怎么做                    | 优点                        | 缺点                                     |
| ----------------------------------------------- | ---------------------- | ------------------------- | -------------------------------------- |
| **A. MediaRecorder + `canvas.captureStream()`** | 逐帧 `BV.step()` 推进，同时录制 | 代码量小（几十行）、体积小             | 输出 webm；**iOS Safari 支持不完全**；帧率受录制时钟影响 |
| **B. WebCodecs + 自己封装 MP4**                     | `VideoEncoder` 逐帧编码    | 可控、可出 MP4                 | 需要自己写 muxer，兼容性同样有坑                    |
| **C. 逐帧 PNG 打包 ZIP**                            | 每帧出一张 PNG              | **零兼容问题**，任何浏览器都能跑；用户可自己剪 | 体积大；用户要额外一步做视频                         |

**建议：先做 C 作为保底（今天就能做，绝对能跑），同时做 A 作为默认路径。**  
关键实现细节（无论 A 还是 C）：

- 录制必须**确定性驱动**：`BV.anim.t0` 挪到远未来，然后按固定步长 `BV.step(t0 + f*16)` 推进。  
  不然真实时钟抢帧，录出来的动画会抖。  
  （这个坑项目里已经踩过并写在技能里了，直接复用。）
- 时长按 **一秒 4~6 页**编排；整本 28 页 ≈ 6~8 秒，正好是短视频的舒适长度。
- 片尾留 1.2 秒定格封面 —— 短视频里"最后一帧"就是封面。

---

## 5. 少女感的「纪律」：做什么 / 不做什么

可爱来自**比例和克制**，不来自颜色浓度。这条如果没守住，做出来就是"廉价拼图 App"。

### 要做

| 项         | 具体做法                                              |
| --------- | ------------------------------------------------- |
| **圆角**    | 全局圆角基准从现在的偏小值提到 14–18px；卡片、按钮、贴纸统一走 `skin.radius` |
| **字重**    | 中文标题用 500–600，正文 400；**不用 900 超粗**（超粗 = 廉价感的第一来源） |
| **字体对**   | 每套 skin 只配 3 个角色：标题衬线/圆体、正文无衬线、手写体。**全套不超过 3 种**  |
| **留白**    | 保持现在的克制。氛围皮肤只换颜色和字体，**不改版式比例**                    |
| **动效**    | 只加两种：落页的轻微回弹、贴纸吸附时的一下"啵"。其余不动                     |
| **空状态文案** | 从功能说明改成邀请语气（"先放一张照片进来吧"），但要短                      |
| **颜色**    | 每套 skin 只允许 1 个强调色 + 1 个浅色辅助，其余全是纸色与墨色            |

### 不要做

1. **不要满屏粉** —— 粉色只做 accent，纸底仍是白/奶油/雾紫；
2. **不要 emoji 堆砌** —— 每屏 ≤ 1 个，且只在图标位用；
3. **不要彩虹渐变 / 描边字 / 阴影字 / 艺术字**；
4. **不要超过 3 种字体**；
5. **不要为了"少女感"动版式** —— 版式是骨架，骨架一变就回到"拼图 App"；
6. **不要加"炫光""闪烁""粒子"** —— 那是 2010 年的审美。

---

## 6. 手机端：本方案最大的风险（需要拍板）

### 现状（已在当前构建里核实，不是猜测）

`head.html` 的两个断点做了这件事：

```css
@media(max-width:1000px){ .rail{display:none} }     /* 素材栏直接隐藏 */
@media(max-width:800px){  .panel{display:none} }     /* 设置面板直接隐藏 */
```

也就是说，**在手机上打开，你得到的是一个"能导入 → 能生成 → 能翻书"的通道，  
但素材栏和设置面板整个消失** —— 逐张分配模版、勾选入册、导出、所有设置，全都够不着。

而目标人群（小女生）**绝大多数在手机上使用**。所以：

> **如果手机端不做，本方案里所有"少女向"的功能都等于没做。**

### 两条路线

**路线 A：把现有布局做成响应式**  
把素材栏、设置面板、胶片带、3D 选片条全部重排到手机尺寸（抽屉 / 底部弹层 / 手势）。

- 优点：一套代码，功能完整对等。
- 缺点：工作量是本次方案里最大的一块，且 3D 选片条这种横向立体交互在手机上要重新设计。
- 估量：相当于本轮全部 P0 功能加起来的工作量。

**路线 B：手机端走一条"简版流程"（我倾向这条）**  
手机端不追求功能对等，只保留一条最短路径：

```
导入照片  →  选氛围（6 选 1）  →  一键成书  →  全屏翻书  →  导出竖图 / 视频
```

编辑能力（逐张模版、贴纸、细致排版）收进一个「精修」入口，  
并明确提示"建议在电脑上打开获得完整体验"。

- 优点：成本约为路线 A 的 1/3，而且**这条路径恰好就是"记录型"用户的真实需求** ——  
  她在手机上要的是快出片，不是精修。
- 缺点：手机上做不出"一本独一无二的书"，只能做出"一个好看的模板产物"。

**我的建议：路线 B 作为第一步，路线 A 作为后续。** 理由：先把"她会在手机上用"这件事跑通，  
拿到真实反馈，再决定要不要为手机端补齐精修能力 —— 反过来做（先啃响应式）风险高得多。

---

## 7. 工程侧顺带该修的（不是新功能，但不修会拖后腿）

| 项             | 现状                                                   | 建议                                                  |
| ------------- | ---------------------------------------------------- | --------------------------------------------------- |
| **单文件体积**     | 2.99 MB，其中约 2.8 MB 是 28 张示例照片的 base64                | 示例照片降采样 + 转 webp 再内嵌，或改成"按需从网络取（带本地兜底）"。目标 < 1.2 MB |
| **生成耗时**      | 28 张 × 1600px 长边，串行渲染                                | 现在有进度提示，够用；若加到贴纸层要留意叠加后的耗时                          |
| **Safari 兼容** | MediaRecorder / WebCodecs / `ctx.letterSpacing` 都不可靠 | 视频走 §4.6 的保底方案；文字字距项目里已自己实现 `tracked()`，继续保持        |
| **字体授权**      | 手写体与圆体的商用授权要单独确认                                     | 上线前逐个体检，**这是法律风险不是体验问题**                            |
| **触屏手势**      | 现在只有 6 处 pointer 事件，双击缩放/双指缩放没有                      | 手机端必须补：双指缩放、单击翻页、左右滑                                |
| **触觉反馈**      | 无                                                    | 翻页与吸附时加 `navigator.vibrate(8)`（安卓有效，iOS 忽略），成本一行    |

---

## 8. 怎么验证「真的吸引到了」（不要靠感觉）

| 判据              | 现状                                | 目标                             |
| --------------- | --------------------------------- | ------------------------------ |
| 从打开到"第一张能直接发的图" | 载入示例 → 生成 → 切到导出 → 下载 = **4 步以上** | **≤ 3 步**，且第一步是"选氛围"           |
| 导出物能不能直接发       | 3:4，书页尺寸                          | 竖版 1080×1440，≤ 3 MB，**无需二次裁切** |
| 第一屏的决策成本        | "选模版"（11 选 1，还要理解模版是什么）           | "选氛围"（6 选 1，看颜色就知道）            |
| 分享物长度           | 只有静态长图                            | 视频 6–8 秒 + 竖版长图两种              |
| 可埋点验证           | —                                 | 氛围切换率、贴纸使用率、视频导出占比、二次打开率       |

**最关键的一条**：拿给 3 个目标用户（不用教程、不给提示）用一次，  
看她们**第一步会不会卡住**。卡在"选模版"就说明 §4.2 的一键氛围是对的。

---

## 9. 建议的节奏

| 批次      | 主题      | 内容                                         | 状态 |
| ------- | ------- | ------------------------------------------ | ---- |
| **第一批** | **能分享** | 竖版导出 · 分享长图 · 氛围皮肤 6 套 · 一键氛围成书 · 文案库与心情标签 | ✅ 已完成（§9.1） |
| **第二批** | **能玩**  | 贴纸与手写涂鸦 · 氛围音效 · 九宫格小卡页 · 日期戳/票根 · 双封面寄语   | ✅ 已完成（§9.1.8，第十三轮）<br>🔧 第十四轮补做：贴纸坐标系 + 立刻生效（§9.1.9）<br>🔧 第十五轮补做：照片矩形过 CTM（§9.1.10） |
| **第三批** | **能留住** | 视频导出 · AI 一键美化 · 双人本 · 搭配分享                | 未开始 |

第一批刻意**不含贴纸**：贴纸是新数据模型，而第一批的价值在于"立刻能分享出去"。  
先把分享链路跑通，拿到真实反馈，再决定贴纸要做到多深。

> 第二批的排序也被照原样遵守了：五项都是**做完就能立刻看见**的东西（贴纸烧进成片、九宫格是版式、
> 寄语在封面上、音效在翻页当下），没有一项需要等第三批的链路先通。

### 9.1 落地情况（第一批 · 第二批均已落地）

> 第一批见下表；第二批见 §9.1.8。中间 §9.1.1 ~ §9.1.7 是两批之间按用户反馈插入的补做
> （材料质感 / 文案失效 / 图注字体 / 折角错位 / 改名 / 发布尺寸 / 图标重做）。
> §9.1.9 是第二批交付后按用户反馈的补做（贴纸坐标系 + 改完立刻生效）。
> §9.1.10 是紧接着的第二次补做（牛皮纸「贴纸只能往左上拖」= 照片矩形串了坐标系）。

| 项 | 落地形态 | 关键决定 |
| - | ---- | ---- |
| 氛围皮肤 | **7 套**（原味 = 出厂默认，观感与加皮肤前完全一致 + 奶油日记 / 蜜桃气泡 / 雾紫梦境 / 薄荷苏打 / 夜樱 / 法式午后） | 皮肤只给 token，**不复制模版**；11 套模版按 `tone`（light/dark/photo/kraft）从 skin 派生纸色，改成 77 份配置就等于自毁。唯一写入点 `applySkin()` 同时改 `state.opts`（印片）与 `state.book`（书页/书封）——只改一边会出现"书页变粉了、印片还是米色"的割裂 |
| 一键氛围成书 | 首屏 / 逐张分配 / 选片成书三处入口；一次点击 = 套氛围 → 按推荐序列铺模版 → 自动挑封面（复用 `coverScore`）→ 直接生成。带「换一版」 | 每套氛围带一组 `tpls` 推荐序列与一个 `rec.layout` 推荐版式；「换一版」只换排法（序列起点 +1、封面顺延、版式轮换），**不换氛围、不换照片**。它没有任何后门，只是替用户把面板上的动作点了一遍，所以「逐张可控」的前提没被破坏。一张都没勾选时默认"全部做进去"，但按钮文案会先说明 |
| 文案库 + 心情标签 | 6 组情绪 × 7 条（共 42 条）；每条支持 `{name}/{n}/{total}/{mood}/{date}` 占位符，同一句套在 28 张上各自不同。面板里挑一句自动跳下一张 | 图注默认值从 `name` 改成 `note`（没写就退回照片名，老用户体验无变化）——默认值不改的话用户写了半天文案书上一个字都不显示，这个功能就白做了。改文案只重排书页、不重出成片（否则没人愿意写） |
| 竖版导出 | 4 档预设（小红书 1080×1440 / 故事全屏 1080×1920 / 方图 1080×1080 / 明信片 1080×1620）+ 竖版分享长图 | 预设改的是**成片本身的画幅**，不是导出时套白边（后者会得到"一张带白边的方图"，等于还要再裁，不算能直接发）。书页比例与成片画幅解耦，换 9:16 不会把整本书压成手机屏 |
| 单文件体积 | 1.093 MB（含 28 张示例照片），余量 ~109 KB | 加了压缩管线：terser 压 JS（顶层名不混淆，`window.LUMEN` 与顶层标识符存活）+ 保守 CSS 压缩（只去注释与行首缩进，`calc()` 内部空白一律不动）。`--no-min` 可出未压缩版调试 |

#### 9.1.1 补做：材料/质感层（第七轮修订）

用户验收第一版氛围后给了直接反馈：**「就是感觉单单纯纯的换了个色，没有任何质感，比如书面质感，写实真实质感」「这个换一版功能用起来也很单一……不行就去掉吧」**。

判断：**不是氛围没用，是"氛围 = 一套配色"这个模型太薄**。于是补一层**材料**，而不是删掉功能。

| 改了什么 | 落地形态 |
| --- | --- |
| 材料层 | `MATSETS` 7 套配方 = 封面料 + 内页料 + 题字工艺 + 纹理/光泽/光照倍率。贴图 256×256 过程生成（`matTile`），**确定性**（xorshift，同 seed 逐像素一致，否则重排书页时纹理每次都会闪）且**可无缝平铺**（特征在 ±256 四个方向补画） |
| 书页/书封 | 从"纯色 + 一层颗粒"换成三层：底色（皮肤的色号）→ 材料 tile → 光照（受光/背光、书沟阴影、纸边厚度）。封面另加漫射光 + 四角渐暗 + 板缘厚度 |
| 封面题字 | `pressTreat`：压凹（暗影 + 亮边）/ 烫金（皮肤 `foil` 色做多段渐变 + 高光）/ 素题。暗封面上的差别一眼可见 —— 这是"书"与"一张印了字的纸"最大的差别 |
| 材料可改 | 面板里给**真材料小样**（92×30 canvas 直接调 `boardGround` 画）。换材料**只重排书页、不重生成**（材料是装帧层，刻意不烧进成片），也不会把成片判过期 |
| 「换一版」 | 从"挪模版顺序"升级为"换整本书的装帧"：**材料 + 版式 + 模版序列起点 + 封面**四件一起换，但不换氛围、不换照片 |

**踩过的三个坑（都留下了回归守卫）**：

1. **丝绸贴图不可平铺**：最初用 4 条对角渐变铺满整张 tile，渐变在 tile 边界不连续 —— 880px 的书页会平铺它三四次，于是每 256px 一条竖接缝（实测列均值跳变 26.8，是相邻列的 5.4 倍）。改成**整周期正弦**（波数取整数，周期正好等于 tile 边长）后天然接得上。
2. **判"可平铺"的度量本身错过一次**：先用"接缝 vs 相邻列均值"，会把帘纹纸每 4 行的正常大跳误判成接缝；又先写成"接缝 vs 最大跳变"但忘了把接缝那一对排除在基准外，导致比值恒 ≤1（永远放行）。最终判据：**接缝跳变 vs 贴图内部（排除接缝本身）的最大跳变**，并加了一组**反向对照**（人为造一张整幅渐变的坏贴图，必须被判为"有接缝"×114）来证明这个检测器真的抓得住。
3. **封面二次叠印造摩尔纹**：封面本来会把 tile 叠两遍（第二遍 1.7× + 旋转）来搅掉平铺周期 —— 对织纹/纤维是对的，但对"连续波"的丝绸会互相干涉出**菱形格**（暗封面上一眼可见）。现在只对会露出周期的粗料做第二遍。

**本轮验证**：9 个测试套件 / **278 项检查全绿**（新增 `test_mat.mjs` 50 项；`test_auto` 48 · `test_book` 40 · `test_skin` 33 · `test_real` 27 · `test_note` 25 · `test_flow` 21 · `test_export` 21 · `test_landing` 13）。视觉取证见 `.workbuddy/tmp/shotsmat/`（材料样张 / 逐料放大 / 题字工艺 / 书本换料前后）。
（这是材料层那一轮的计数；`test_note` 之后在 §9.1.3 增到 31 项，**当前总数 284**。）

#### 9.1.2 补做：修掉"文案选了没效果"（书页图注整段是死的）

用户反馈：**「为什么这个文案功能没有用啊，选择了怎么显示出来没效果」**。

| 项 | 内容 |
| --- | --- |
| 现象 | 在「选片成书」点一句文案 → 面板显示已写入 → **书页上一个字都没有** |
| 根因 | 图注的抑制条件 `plateHasText` 写的是"印片有没有字"（`!!im.title`），而成片的 `title` 来自 `opts.title`，**出厂就是 `{name}`** → 每张成片都带 title → 条件恒为真 → 图注永不绘制。而模版那行题字显示的是**照片名**，所以用户写的文案在整本书里**没有任何地方出现过** |
| 修法 | 抑制逻辑改比**内容**：文案 ≠ 印片题字 → 画；文案 = 印片题字（`opts.title` 用 `{note}` / 图注来源选「照片名」）→ 不重复。语义仍是「同一信息不出现两次」，但不再退化成"恒不画" |
| 第二处死路 | **「满版出血」版式整段没有画图注的代码路径** —— 写文案 + 选满版 = 同样一个字都不出现。现在满版也画（底部柔光带 + 白字，位于页码上方） |
| 顺手加固 | `two` / `sticker` 的图注原本没有自动缩字，手打长句会顶出版心；四版式现在共用 `fitCap()`（先缩字号，到下限再截断） |
| 两个"假通过"的测试 | ① `test_note` N10 只数图注带里的墨点 —— 印片和照片本身就有墨，凑够墨点数就"通过"了；改成**同页"有文案 vs 无文案"的带内像素差**。② `test_book` B30 把错误语义写成了断言；改成"不是同一句话就必须画"，并加**反向对照**（把旧实现 patch 回产物，断言新守卫在那份上必须失败 —— 实测旧实现下四版式图注带差异全为 0） |

**顺带修掉的一个真 bug**：`.note` 这个类名被两处共用 —— 说明段落 `<p class="note">`（面板里 14 处）与文案候选按钮 `<button class="note">`。按钮样式里带 `white-space:nowrap` + `overflow:hidden`，而 CSS 里光写 `.note` 会把说明段落一起命中，于是**面板上每一段说明都被截成了一行**（`scrollWidth` 1172px 挤在 283px 里）。修法是把按钮样式收进 `.notes .note`，并给说明段落显式声明 `white-space:normal` + 去框。回归测试里加了 4 条守卫（说明段落必须换行 / 无边框 / 不被截断，候选按钮必须仍是按钮形态）。

**本轮明确不做**（按拍板）：手机端响应式（只做桌面流程）；手写体/圆体的商用授权（体系里 0 个 `@font-face`，全部走系统字族，只用于展示）。

#### 9.1.3 补做：图注字体（"太板正、还跟右边不一样"）

用户反馈：**「显示的字体太板正了，不太好看还跟右边显示的字体不一样」**（附截图）。

| 项 | 内容 |
| --- | --- |
| 现象 | 书页图注的中文是**等笔宽的黑体**（和界面同款），而同一跨页右页的拉丁图注是 Georgia（轻、圆、有字距）—— 一眼"两个世界" |
| 诊断方法 | 本机**复现不了**：本机同一行代码落到的中文是**横细竖粗的宋体**。做法是先从用户截图反推显示比例（8 个字占 101 px → 字号约 11.4 px → 880 画布 ≈ 0.50），再用真实无头浏览器把窗口调到同一比例（window 1850×1100，`BV.pw≈456`）截图对照；并做了对照实验"宋体过一遍 JPEG 后细横会不会消失"（不会）→ 排除"压缩吃掉细节"，坐实"用户环境落到黑体" |
| 根因 | **静默降级**：`Georgia,"Songti SC","SimSun",serif` 里没有一款能被浏览器保证找到，缺了就用默认字体顶上且**不报错**。同一份产物在不同环境看到两种字，代码里却没有任何地方记录这件事 |
| 修法① | 字族按用途分角色：`serif` 只留给**题字**（封面/书名页/版权页/导出页眉），新增 `cap` 给**正文**（书页图注），首选**楷体**（Windows `KaiTi` / macOS `Kaiti SC`·`STKaiti`）；拉丁仍走 Georgia（用户认可右边那行，就不动它）。楷体比宋体轻，图注墨色 `.84 → .90` 补回观感 |
| 修法② | 开机用**位图探测**逐个验证候选中文字族是否真的装了（同一个"中文 + 拉丁"样本 vs 一个绝对不存在的字族，像素有差才算在），挑第一个确实装了的；一个都没有时**不退给 `serif` 去猜**，明确退到界面同一套黑体 |
| 修法③ | **面板把实际用到的图注字族写出来**（"图注字体 楷体"）—— 这一行就是这一轮排查时最缺的信息，缺了它"落到黑体"对用户和开发者都是隐形的 |
| 反向对照 | 把字族改回旧的 `serif` → 新断言 N30 必须挂；把一款**本机不存在**的中文字族写在前面（复刻用户环境）→ 中文落到黑体 → N28 必须挂。实测两种情形都如期失败（`.workbuddy/tmp/_neg_font.mjs`） |

**本轮验证**：9 个测试套件 / **284 项检查全绿**（`test_note` 25 → **31**：新增字族探测反向对照 / 图注中文不是黑体 / 拉丁仍是 Georgia / 图注与标题是两个角色 / 面板写出实际字族；其余 `test_mat` 50 · `test_auto` 48 · `test_book` 40 · `test_skin` 33 · `test_real` 27 · `test_flow` 21 · `test_export` 21 · `test_landing` 13）。
视觉取证：工作区 `图注字体-改前改后.png`（你的截图 / 黑体 / 宋体 / 楷体 同尺度四联）、`图注字体-修复后.png`（真实浏览器整页）。

#### 9.1.4 补做：悬停折角跑到书沟上

用户反馈：**「图书翻页有问题吧，怎么会在那个位置上出现呢」**（附截图）。

| 项 | 内容 |
| --- | --- |
| 现象 | 悬停页面下缘掀起的"折角"出现在书的**中缝（书沟）**上，而不是书的**外沿（书口）**。用户图里那一角正好翘在两页中间，看着像右页内下角凭空折了一下 |
| 定位方法 | 一开始用"找浅色三角"量不到（那片是渐变，阈值挑不出边界）。改成**同一帧「开折角 / 关折角」两张图的像素差**，差的 bbox 就是折角真正落在哪 —— 悬停左页落点 `x∈[463,515]`，而书沟正好 463、左页书口边 99，**错得很干净** |
| 根因 | `drawFold` 的锚点写成 `right ? this.spineX + pw : this.spineX`，**左页那半边少了一个 `- pw`**。`spineX` 在合书时是"当前页左边"、跨页时是"**书沟**"——语义随模式漂移。左页折角要的是左页**书口边**（外侧）= `spineX - pw`，写成 `spineX` 就落到书沟上了；右页那半边本来就是 `+ pw`，所以一直正确 → 症状表现为"只有左页会错" |
| 修法 | `right ? this.spineX + pw : this.spineX - pw` —— 两侧都显式取各自的书口边 |
| 反向对照 | 新增断言 **④**（两侧折角必须贴书口边、且必须离开书沟）+ **④b**：运行时把锚点**还回书沟**，同一判据必须判不合格（实测旧写法 `x∈[468,516]` vs 书沟 469 → 如期挂）。没有 ④b，④ 有可能只是"恰好通过" |

**本轮验证**：9 个测试套件 / **286 项检查全绿**（`test_landing` 13 → **15**：新增「悬停折角落点在书口边」与「反向对照」，其余计数同上一节）。
视觉取证：工作区 `翻页折角-修复对照.png`（改前悬停左页 = 折角在书沟 / 改后 = 折角在左页书口 / 改后右页 = 折角在右页书口，三联同尺度）。

---

#### 9.1.5 补做：品牌改名 + 图标（光匣 LUMEN → 咔哒书 KADA）

| 项 | 内容 |
| --- | --- |
| 名字 | 「咔哒」是拟声词 —— 快门那一下，也是合上书那一下，两个意思正好压在这个产品的两半上。气质从"文气"转"俏皮"，也更贴原本的目标用户 |
| 图标（**第十二轮已更换**） | 沿用原来的形状资产（**圆角方块 + 琥珀金**），把里面的「匣」字换成一个真正的图形：**一本摊开的书，从方块里挖空出来**。⚠ 这一版**被用户否掉了**（原话「logo不是很好看呀」），现状见 §9.1.7（手绘小书）；名字与迁移逻辑不受影响 |
| 关键决定 | 图形走 **evenodd 挖空**（不是叠色）→ 挖空处透明，深底上是"深书"、浅底上是"白书"、换皮肤时是"皮肤色的书"，**一个文件适配所有底**；纯矢量**不依赖字体**，顺手解决了 §4.9 那个"系统没有这个字族"的隐患 |
| 选型过程 | 试了 20+ 个方案：圆环+书 / 六叶光圈 / 书即光圈 / 右页掀起 / 折角页 / 方块+亮色书…16px 下**「圆环+书」糊成一团、「书即光圈」读成罗盘、「右页掀起」读成对话气泡**。最后选的是最"笨"但小尺寸最稳的那一版 |
| 改名范围 | 所有**用户可见**文字：标题、顶栏、默认书名/署名/书脊、页眉页脚小字、导出文件名与压缩包说明、沉浸阅读标题、示例照片名（`KADA 01..28`）、环衬那枚印（「匣」→「咔」） |
| 故意不改 | 存档 key `lumen.prefs.v1`（改掉老用户**丢存档**）、JS 命名空间 `window.LUMEN`（只加 `window.KADA` 别名，改内部命名要把整套回归跟着改，收益为零） |
| 老存档迁移 | 书名/署名/书脊**原样等于旧默认值**的换成新名，用户自己改过的一律不动 |
| 踩的坑 | 第一版迁移把 `title/author/spine` 当成了 `opts` 的字段 —— 它们其实是 **`book`** 的。`DEFAULT_OPTS.author` 是 `undefined` → 迁移一跑就把字段**抹成 undefined**，回归里表现为"字段从 JSON 里消失了"（`JSON.stringify` 丢掉 undefined）。顺带把品牌名收成 `BRAND`/`OLD_BRAND` 常量，"默认值改了、迁移里还写着旧名"这种脱节就不会再发生 |

**本轮验证**：9 个测试套件 / **287 项检查全绿**（`test_book` 40 → **44**：新增改名迁移 4 项，其余计数不变）。
> 287 是从第十二轮的实测值往回推的：303（第十二轮）→ 296（第十一轮 +9）→ 287（本轮 +4）。
> 当时文档写的是 290 —— 比实测多 3，因为 `test_landing` 一直被当成 15 项，实际一直是 12 项。
视觉取证：工作区 `咔哒书-LOGO方案.html`（完整品牌方案表：顶栏实况 · 多尺寸 · 构造图 · 备选方向 · 字标 · 改名清单）、
`咔哒书-品牌改前改后.png`（同一窗口尺寸的顶栏对照）。

#### 9.1.6 补做：「发布尺寸没效果」（第十一轮）

| 项 | 内容 |
| --- | --- |
| 用户反馈 | 「这个导出的发布尺寸好像没有什么不同，具体的效果貌似并无什么效果」 |
| 现象 | 点「方图」→ 导出 ZIP → 里面的图**和改之前一模一样**（960×1280），而文件名和 README 却写着 1080×1080 |
| 根因① | `exportZip()` 只检查 `state.generated.length`（有没有成片），**从没检查它是否过期** → 旧画幅被照发 |
| 根因② | 命名与 README 取**当前设置**而不是**画布实际尺寸** → 文件在"自己是什么尺寸"上撒谎 |
| 修法① | 新增 `ensureFreshArt()`：过期就先按当前设置重出一批再打包（**只是替用户点了一遍「生成成片」，不新造旁路**，符合硬约定 10）。`导出全部成片（ZIP）` 与 `下载当前成片` 都走它 |
| 修法② | 命名一律从画布读（`g0.canvas.width`）；只有预设像素与真实像素完全相等时才显示预设名，否则显示「自定义 W × H」——**即使守门哪天失效，文件名也不会说谎** |
| 顺带收尾 | 面板在**改尺寸的当下**就给出可见提示（"现有成片是 960 × 1280，和上面选的尺寸（1080 × 1080）不一致…"）；过期原因列表补上「发布尺寸」（旧文案只列了素材/模版/调色，改尺寸的用户看到那段提示完全对不上自己的操作）；弹出提示从「点『按当前设置重新生成』出片」改成「导出时会先按新尺寸重出成片」 |
| 为什么原来没拦住 | `test_export` 的 E5/E8 断言的是**设置变了**，而用户报的是**文件没变** —— 断言停在了"用户能看见的那一层"的**上一站** |
| 新断言 | 在页面内替换 `URL.createObjectURL` 拦下 ZIP，再**自己解包**（扫 Local File Header + `DecompressionStream('deflate-raw')` + 读每张 JPEG 的 `SOF`）拿**真实像素**；四档预设逐档验"改完直接导出"；外加**反向对照**（把守门替换成 `if(false)return;` 另存一份，同一操作必须发出 960×1280） |

**本轮验证**：9 个测试套件 / **296 项检查全绿**（`test_export` 21 → **30**）。取证图：工作区 `咔哒书-发布尺寸-修复对照.png`。

**通用教训**：凡是「设置 → 渲染 → 文件」的链路，断言要打在最末端（字节里的像素），
不要打在最前端（设置值）—— 前端那一站改了，末端完全可能没跟上。

#### 9.1.7 补做：图标重做 —— 手绘小书（第十二轮）

| 项 | 内容 |
| --- | --- |
| 用户反馈 | 「logo不是很好看呀，有没有类似这种的」+ 一张 **16×16 的手绘小鱼**截图。**那是风格参考，不是要一条鱼** |
| 先做的事 | 把 16px 参考图**逐像素量了一遍**（亮度矩阵），反推出笔宽/填充亮度/对比度/细节数。第一版"看着不对劲"全是**算术问题**：笔宽粗了 50%、填充亮了 47。改数值，不改灵感 |
| 定稿 | 48 网格的一本摊开的书。绕 `(24,24)` 缩放 `sx 1.055 / sy 1.17` 补回旧方块的视觉分量 → `translate(0 1.05)` 居中 → **`rotate(-13 24 24)`**。填充暖棕渐变 `#eec9a4→#d7a578`，描边与页线 `#4a2708` |
| 关键决定 | 图标**不跟强调色走**。被换掉的那版 `fill="currentColor"`，换氛围皮肤/切浅色主题时整枚图标跟着变色 —— **品牌色如果是"当前皮肤色"，它就不成其为品牌色了**。现在它固定暖棕，像一枚贴在界面上的实物贴纸 |
| 顺带 | 顶栏投影从 `0 5px 15px var(--glow)`（琥珀光晕）换成 `0 2px 5px rgba(0,0,0,.34)`：光晕配棕色实物感有点冲；favicon 换成同一份矢量（内嵌 data-URI，**仍是单文件离线**） |
| 踩的坑① | **别在"预览页"里看图标**：真产物里 `.brand .logo` 的 29px 是 `.brand` 包裹层的 CSS 给的，少了那层包裹，SVG 会按 viewBox 原尺寸铺开、四周一圈空，好图标看着像废的。**要预览就贴进真产物**（正则在产物副本里替换那一段） |
| 踩的坑② | **内联 SVG 的 `id` 必须逐份唯一**：`linearGradient id="kadaLogoG"` 被复制多份时浏览器只认第一份，改一份、另一份跟着变，排查方向会跑偏 |
| 踩的坑③ | 16px 位图**只能逐像素量，不能靠放大截图看** —— 插值会把 1px 抖动渲染成"设计感" |
| 新断言 | `test_book` 新增 7 项：29×29 / 新路径在且**旧方块路径必须不在** / 渐变端点 / **栅格化后数像素**（有墨、是暖色、旧方块色为 0；纸色同理）/ favicon 与顶栏同源 + `xmlns` + 2 stop / favicon 16px 栅格化描边与填充都在。数像素那两项**自带反向对照**：同一段代码去数旧方块必须数出 0 |

**本轮验证**：9 个测试套件 / **303 项检查全绿**（`test_book` 44 → **51**）。
取证图：工作区 `咔哒书-图标对照-落地后.png`（改前/改后 × 深色/浅色，3× 实拍，**四张取景完全相同**）、
`咔哒书-LOGO方案.html`（唯一一份方案页，第十轮那份方块图标的已归档）。

**通用教训**：
1. 有人说"照着这张图做"时，**先把参考图量出来**（它多大？笔多粗？对比多强？细节几个？）。
   凭感觉画的版本会被否，而且否掉之后你不知道该往哪边调 —— 量过之后"粗了 50%"是个可执行的数字。
2. **对照图的公平性来自"同一取景"**，不是事后对齐。改前改后必须同窗口、同缩放、同裁切框，
   否则读者的注意力全在"这两张怎么不一样大"上。

---

#### 9.1.8 第二批「能玩」落地（第十三轮）

五项一次做完，因为它们共用同一块地基 —— **一件贴纸的数据模型**（归一化坐标 + 烧进成片）。
其余四项都是它的邻居：九宫格是一种**版式**、音效是一种**交互反馈**、双封面寄语是两处**文字位**、
而日期戳与票根本来就是贴纸里的两枚。

| 项 | 落地形态 | 关键决定 |
| - | ---- | ---- |
| 贴纸（8 枚） | 和纸胶带 / 小星星 / 小爱心 / 闪光 / **手写圈** / **日期戳** / **纪念日戳** / **票根**。每枚 `{k,x,y,rot,s,text}`，`x/y` 是**相对照片**的 0..1，一枚一只，一册最多 6 枚 | ① 坐标归一化是这一整块的根：一份数据同时喂缩略图 / 胶片 / 成片 / 书页，换画幅不用换算表。② **贴纸烧进成片**，不是做成书页上的一层 —— 书页画面有"模版成品 / 干净照片"两态，贴纸若只画在书页上，用户一切到"干净照片"贴纸就全没了。烧进成片后 `book.art` 怎么切都在 |
| 手写涂鸦 | 预置笔迹的**手写圈**（可改字，默认"好喜欢这天"）+ 圈里的用户文字 | 拍板过不做自由画笔（§10 建议①：成本三倍、产出不如预置笔迹）。所以形态是"选一个圈、写一句话"，覆盖"我想在这张照片上说句话"的九成 |
| 氛围音效 | `Sound.swish`（掀页）/ `land`（落页）/ `pop`（贴上一枚贴纸），纯 WebAudio 程序生成（**零素材、零请求**）；顶栏与面板两处开关，开关进存档 | **音频是装饰**：每个入口都 try/catch 包住 —— 绝不许因为没声卡 / 没授权 / 浏览器策略而打断一次翻页。默认开，但必须能关、且记得住；顶栏按一下，面板里的勾要跟着变（两处入口一个状态） |
| 九宫格小卡页 | 第 5 种版式 `grid`：3×3 白卡（带投影与细边）+ 封面填充的方形小图 + **每格保留自己的小图注**，一页 9 张 | **刻意不进任何 `SKINS[*].rec.layout` / `alts`** —— 一键成书是给"不想做决定的人"用的，而 28 张照片铺成九宫格联系表不是相册。它**可以被用户主动选**，但不被推荐。测试同时守两条：0 套皮肤推荐它 + 它确实在 `LAYOUTS` 里 |
| 双封面寄语 | `book.coverNote`（封面书名下、署名上）/ `book.backNote`（封底 `END OF VOLUME` 细线与条形码之间，**最多 3 行**，超了收省略号） | 默认空串 = 老存档像素级不变，**所以这一项不需要写迁移**。空白时那一段一个像素都不画（回归里量的是"与确定性重渲染的差异 = 0"） |

**三个"为什么这么做"**：

1. **为什么贴纸挂在照片上，而不是挂在页面上。** 页面级贴纸要处理一个反向映射：
   用户在页面上点了一个位置 → 这个位置落在第几格 → 那一格里的照片偏移是多少 → 归一到照片坐标。
   这是方案 §4 里**明确推迟的第二个难点**（第一个是自由画笔）。挂在照片上还有一个白送的好处：
   贴纸跟着照片去封面、去分享长图、去 ZIP 压缩包，**一行额外代码都不用写**。
2. **为什么必须给一块"照片比例的画布"。** 贴纸坐标是照片相对的，而它在页面上出现在哪还取决于版式
   （满版 / 居中 / 双图 / 九宫格）。没有这块预览，用户就是**盲贴** —— 在缩略图上点的位置，
   成书里可能跑到别处去。画布直接用照片自己的长宽比，于是 canvas 坐标 ↔ 贴纸坐标是 **1:1**，
   不需要换算表，也就不会出现"预览对、成片错"。
   ⚠ **这一条在第十四轮被推翻了**（§9.1.9）：`1:1` 只对"照片铺满画布"的那一版成立，
   而模版会给照片留白内缩 —— 于是"预览对、成片错"恰恰就这样发生了，且是必然发生。
   现在这块画布画的是**那张成片本身**（含模版），落点锚在**照片矩形**上。
3. **为什么 `plateHasText`（同一信息不出现两次）不套用在贴纸上。** 那条规则管的是"题字与图注别重复"，
   而贴纸是**用户亲手贴上去的**东西 —— 他觉得重复也是他要的。把"用户明确放的东西"也一起抑制，
   就又回到"改了却看不见"的老毛病上。

**踩到的四个坑（都留了回归守卫）**：

| 坑 | 症状 | 根因 / 修法 |
| - | ---- | ---- |
| 局部变量 `R` 撞上全局取整函数 | 一画「纪念日戳」就崩，而**面板上那一格只是空白**（不报错、不弹窗） | `stkAnniv` 里 `const R=S*.70` 撞了 `core.js` 第 5 行的 `const R=n=>Math.round(n)`，后面 `R(size)` 变成"调用一个数字"。**面板每个小样都包在 try/catch 里**，所以它崩在没人看得见的地方，只在真画一枚纪念日戳时才炸。改名 `Rd` |
| 九宫格铺成了 2/3/3/1 | 第 9 格跑到版心外面去了 | `R(i/3)` 被当成行索引 —— 但 `R` 是**四舍五入**不是取整：i=2 → 行 1、i=8 → 行 3。改成 `Math.floor(i/3)`。（我自己的探针里同一天犯了同一个错：`H*.098` vs `H*.082` 把版心上边距算错了） |
| 贴纸小样「和纸胶带」是一片空白 | 面板第一格什么都没有，看着像漏画了 | 和纸胶带是**纸色半透明**的，而小样底色用的就是纸色 —— 同色叠同色。给 `stkPalette()` 补一个 `chip:'#b9b2a6'` 专当小样底色（它是"看样用的台面"，不是调色板的一部分）。⚠ 补完之后第一版断言变成**假绿**：它数的是 `alpha>8` 的像素，把底色一起算进去了；改成数"与 chip 色不同的像素" |
| `s.text` 把"空"和"从没设过"混成一个 | 用户把日期戳的字删光，一松手又跳回默认值 —— 又是"改了却看不见" | `s.text==null \|\| s.text===''` 都回退默认值。改成**只有 `null`（从没设过）才回退**，并且加贴纸的那一刻就把默认文字**显式写进数据**。同一个错误在 `drawSticker` 里还藏着第二份（`s.text \|\| def.dflt`）—— 修完第一处，测试立刻红在第二处 |

**这一轮的取证方法（比结论更值得记）**：

- **量贴纸的几何，必须在白画布上量。** 样例照片是深色抽象渐变，"深色像素的 bbox"**永远是整张画布** ——
  于是"涂鸦变宽了"和"什么都没变"量出来一模一样，两个相反的结论都能"通过"。
  改成把贴纸画到一张填白的画布上、再数非白像素，几何才真的有分辨力。
  这个改动顺带把上面那个 `R` 崩溃**逼了出来**（崩在 try/catch 外面了）。
- **画布里有 `Math.random` 时，量位置要用「两次渲染求差 + 只取第一段连续差异行」。**
  封底有条形码（全篇唯一还在用 `Math.random` 的绘制），两帧之间的差异会一路延伸到页脚，
  把"一句话"的 bbox 拉成整整一页。做法：先在"有寄语 / 无寄语"两次渲染间求差，
  再**只取第一段连续的差异行**（允许中间空 `H*.03` 的间隙）。间隙一开始写成 6px 时，
  它把三行寄语砍成了第一行 → 报出"18px → 19px"这种**假失败**；真实行距约 13px、
  而到条形码有 130+ 行，所以 3% 分得干净。
- **每条断言都要有反向对照。** 清空贴纸后画面必须**逐像素**回到原样；
  同一枚贴纸在 5 种画幅上必须落在同一归一化点（实测 x = 0.740 / 0.739 / 0.740 / 0.739 / 0.738，短边占比同为 18.1%）；
  封面寄语为空时，与"确定性重渲染"的差异必须是 **0 个像素**。
- **滑杆的容差 = 控件自己的 step。** `input[type=range]` 会把 `value` 吸附到 `step=0.01`，
  拿 `1e-6` 去比就会把正常行为报成失败（"滑杆 0.7 / 数据 0.7006"）。容差改成 0.011。

**本轮验证**：**10 个测试套件 / 393 项检查全绿**
（新增 `test_batch2.mjs` **76** 项：贴纸数据层 / 烧进成片（含"清空后逐像素还原"的反向对照）/
五种画幅归一化 + 八枚都真能画 / 面板与交互 24 项 / 音效 / 九宫格 / 封面封底寄语 / 真实 CDP 鼠标拖贴纸；
其余 `test_book` 51 · `test_flow` 21 · `test_landing` 12 · `test_real` 27 · `test_skin` 33 ·
`test_note` 31 · `test_export` 30 · `test_auto` 48 · `test_mat` 50）。
⚠ 这里的 379 是**第十三轮当时的数**；`test_batch2` 在第十四轮增到 **90**，
全量口径随之变成 **393**（见 §9.1.9）、第十五轮再变成 **404**（见 §9.1.10）。
`_gate.py` 由 9 个套件扩到 **10** 个；新增 `_renum.py` 给 `B2-N` 标签按出现顺序重编号
（改过几轮之后标签会重复、乱序，肉眼查不出来）。
产物 **1.119 MB / 余量 83 KB**。
取证图：`.workbuddy/tmp/shotsb2/`（八枚贴纸样张 / 同一张照片"原样 vs 贴满" / 五种版式对照 /
封面与封底寄语 / 贴纸面板实况）。

**通用教训**：
1. **硬约定 6（用户能改的每一项，都要在他最终交付的东西里留下痕迹）在第二批里又应验两次** ——
   `book.art` 默认必须是 `tpl`（否则用户逐张挑的模版在成书里看不见）、
   日期戳的默认文字必须**写进数据**（否则用户清空后它会自己弹回来）。
   两次的症状都是"改了却看不见"，两次的根因都是**默认值选错了**，而不是功能没做。
2. **"设置 → 渲染 → 产物像素"的断言要打在最末端**（§9.1.6 那条的第二次应验）：
   贴纸这一项如果只断言 `state.photos[0].stickers.length === 1`，上面四个坑一个都抓不到。

#### 9.1.9 补做：贴纸「改了看不见 + 位置对不上」（第十四轮）

第二批刚交付，用户就报了两条：

> 「我发现这装饰，我修改不是动态呈现的，添加甚至修改后，需要手动点击生成照片才有，
>  这不太好，甚至是删除贴纸也是这样的」
> 「这个贴纸修改的位置，在已有的照片显示那不同模版显示，会导致贴纸修改的位置对不上，
>  这咋整，**预览里也要是带有模版的照片**？你看看吧怎么弄最合理最好看最好用」

**两条是同一个根因。** 贴纸坐标的**含义**是"归一化到照片"，但落到像素时乘的是**画布**尺寸
（`cx = s.x * W`）—— 而"照片在画布里占哪一块"只有渲染那一刻才知道。实测（画布 960×1280）：

| 模版 | 照片矩形 | 形状 |
| --- | --- | --- |
| `magazine` | `0,0,960,1280` | 满幅出血 |
| `polaroid` | `72,-3,816,1088` | 缩进白框，上方还被裁掉一点 |
| `film` | `96,128,768,1024` | 双向内缩 + 偏移 |

三块互不相同 ⇒ 同一个 `x=0.5` 在三种模版里落在三个不同的地方 —— 不是"可能错"，是**必然错**。
同一个根因也解释了面板与书页的分歧：面板里照片铺满画布，书页里模版把照片缩进版心。

| # | 改法 | 为什么 |
| - | ---- | ---- |
| 1 | **唯一坐标系：照片矩形。** `drawFit()` / `paintPhoto()` 把实际画照片的那块 `{dx,dy,dw,dh}` 返回出来，`renderCanvas` / `renderPlain` 挂到 `c.photoRect` 上离开；锚点 = `dx + x*dw`，基准尺寸 = `min(dw,dh)*0.20` | 让"照片在哪"从**渲染的副产品**变成**一等公民**。顺带把面板、书页、导出三条路统一到同一个答案上 |
| 2 | `drawSticker(x, rect, s, i, im)` **只收矩形**，不给 `(W,H)` 数字留兼容重载 | 一开始写了兼容口子，后来**主动删掉**：留着就等于承认"两套坐标系"合法，下次一定有人在面板里按画布算（那正是这次的病）|
| 3 | **面板画布 = 成片。** 直接 `drawImage` 这张照片的 `g.art`（含模版、含贴纸），所见即所得；再叠一道**照片边界虚线**（白晕 + 深色芯双描，浅色深色照片上都看得见）标出"照片到哪为止" | 这是用户点名要的"预览里也要是带有模版的照片"。掩掉虚线/选中环后，面板画布与成片**逐像素相同**（差异 0 px、最大通道差 0）—— 不说"看起来差不多" |
| 4 | 底片按 `stkBaseSig(p)` 缓存（`id\|name\|tpl\|zoom\|ox\|oy\|rot\|spec\|skin\|opts\|adj`），拖动期间只 `drawImage(底片) + drawStickers(实时)` | 手指的反馈不能等成片重渲染。**签名必须覆盖全部输入** —— 少一项就会出现"换了模版/换了皮肤，底片还是旧的" |
| 5 | `refreshStickerArt(p)` 重出**这一张**并更新 `stick` 签名；`scheduleArt(p)` 120ms 去抖攒批 → `scheduleBook()`/`schedulePreview()` + `renderDock()` + `PERSIST.save()` | 改完立刻生效，`bookStale()` 同时变假（面板不再亮"成片已过期"、导出也不必白重出 28 张）。贴上/拖动/删一枚/清空这一张/清空全部/改文字 **六个改动点全部改走这条路**，不留一个还停在旧行为上 |
| 6 | 指针换算从一步变两步：`客户端px →(÷显示尺寸×画布px)→ 画布px →(−矩形原点)÷矩形尺寸→ 归一化` | 这是"对不上"的另一半。老写法只做了最后半步，面板里照片铺满画布时碰巧对，一内缩就散 |

**为什么不干脆调 `generate()`**：它会**重建 `state.generated`**、重置 `_genSig`，
在第 1 步还可能把用户切到别的步骤去。用户只是挪了一枚贴纸，却要重出全部 28 张（实测 1571ms）、
还要承担状态被重排的风险 —— 那不是"刷新"，是副作用。

**实测成本**（28 张的书）：`refreshStickerArt` 29ms · `buildBookPages`（34 页）89ms ·
`BV.setPages` 41ms。链条 = 120ms 去抖 → 29ms → 40ms 去抖 → 89ms → 41ms；
**页面内时间戳**量到：成片换掉 **170ms**、书页拿到新页 **369ms**。手指抬起来之前书页就换好了。

**这一轮的取证（比结论更值得记）**

| 事项 | 差点写错的结论 | 真相 |
| - | ---- | ---- |
| 延迟 | 第一版检测器量到 **1412ms**，差点写"产品要 1.4 秒" | 是**检测器自己造的**：每 30ms 调一次 `buildBookPages()`，每次都新建约 40 张 880×1173 画布，主线程被分配/回收拖住（两轮轮询实测 616/684ms）。换成读 `BV.pages`（**屏上真在显示**的画布）后，页面内里程碑是 **170 / 369ms**。顺带确认：`BV.pages` 存的是**画布**、`BV.kinds` 存类型，画布上没带页码 → 只能"找第一个内容页" |
| 定时器节流 | 怀疑后台标签页把 `setTimeout(30)` 钳到 1s（`document.hasFocus()` 确实是 `false`） | 量了 30/40/120ms 的实际间隔 = **31/41/121ms**，反复静置也一样 → **假说被自己的数据否掉**。`visibilityState` 是 `visible`，Chrome 没节流 |
| "字形居中" | 断言"贴纸画在 `(.5,.5)` 就该落在矩形正中"，红了 | 心形的墨**天生**比锚点低 **0.086·S**（8 枚全量过：`stamp/anniv/ticket` 是 −0.003，`spark` +0.039/+0.008，心形 −0.003/+0.086）。改成两条与字形无关的判据：**① 平移多少就挪多少**（挪矩形 `(137,221)`，墨迹中心必须挪 `(137,221)`）**② 偏移按 S 归一化后跨矩形一致**（三块不同矩形 `du/dv` 散布 < 0.01）|
| `NaN` 的画 | 测试报 `-1000000001px`，看着像"量程不够" | 是漏改签名的两处（`drawStickers(g,W,H,...)` 传数字 → `rect.dx` 为 `undefined` → 坐标全 `NaN` → **一个像素都没画**）。`-1000000001 = -1 - 1e9` 是 `maxX-minX` 的"没找到墨迹"哨兵值。**看到量级离谱的数，先查"是不是什么都没画"，别去调阈值** |

**本轮验证**：**10 个测试套件 / 393 项检查全绿**
（`test_batch2` **76 → 90** 项：新增贴纸坐标系三组（人造矩形 + 平移对照）、三种真模版矩形互异、
面板画布与成片逐像素一致、"不点生成书页自己变"（含"只重出这一张"与两条反向对照）、
真实按钮删除也立刻生效；其余 `test_book` 51 · `test_flow` 21 · `test_landing` 12 · `test_real` 27 ·
`test_skin` 33 · `test_note` 31 · `test_export` 30 · `test_auto` 48 · `test_mat` 50）。
`_gate.py` 加了"**零断言 + 非零退出 = 套件没起来，重跑一次**"（实测 `test_note` 撞过一次
`webSocketDebuggerUrl is undefined`，单跑就是 31/31 全绿）——重试会**打印出来**，
真崩溃同样是 0/0、重试后仍然 BAD，不会被吞掉。
产物 **1.121 MB / 余量 80.5 KB**（本轮只改渲染坐标与刷新链路，photos 段未动）。
取证图：`.workbuddy/tmp/shotsb2/` 新增三张 —— **`14-贴纸坐标系对照.png`**
（同一份贴纸数据在 `magazine`/`polaroid`/`film` 上各画两遍：① 锚在画布上 vs ② 锚在照片矩形上；
两行里都画出"②的正确落点"作红色准星，①的贴纸明显偏离 ——`magazine` 是满幅出血，
所以它两行重合，那正说明"本来就是对的模版不受影响"）、
**`15-面板画布与书页.png`**（面板画布 / 这张的 `g.art` / 书里那一页 三格并排，前两格同一份像素）、
**`16-改完立刻生效.png`**（改前 0 枚 → 改后 3 枚，只调了一次 `scheduleArt(p)`；
图上标的是里程碑 201ms / 326ms，而同一时刻轮询检测器报到 **890ms** ——
这 2~5 倍的差就是上面那条"检测器造的慢"）。
工作区预览新增 **`预览-13-贴纸面板-所见即所得.png`**（真实浏览器截图：
面板里那块画布就是带模版的成片 + 照片边界虚线 + 新文案）。

**通用教训**：
1. **"坐标归一化"这件事，必须同时说清"归一化到什么"和"从哪个矩形还原"。**
   第十三轮只做对了前半句（数据是照片相对的），后半句用了画布 —— 于是数据模型没错、
   渲染也没报错，只有**位置**是错的，而位置错是最难在代码里看出来的错。
2. **派生位图不能依赖用户点按钮。** 成片是贴纸的下游产物，任何一处改了贴纸就必须让下游跟上，
   否则就是又一个"改了却看不见"（硬约定 6 的第三次应验，也是第三次由"默认值/链路"而不是"功能"造成）。
3. **一句话里有两个抱怨时，先找它们的公共根因。** 这次两条抱怨（不够动态、位置对不上）
   修的是同一行代码；分开修会得到两个补丁，而那两个补丁会互相打架（面板改成成片之后，
   旧的一步换算立刻把"对不上"放大到面板上）。

#### 9.1.10 补做：牛皮纸的贴纸「只能往左上拖」（第十五轮）

§9.1.9 刚交付，用户就报了下一个：

> 「有个bug这个牛皮纸，插入的贴纸无法往右以及往下拖拽」

**根因不在贴纸，在 §9.1.9 那个新引入的中间量上。** 牛皮纸是**先 `translate+rotate` 再画照片**的，
于是 `paintPhoto` 里 `drawFit` 量出来的矩形是**调用点的局部坐标**（`-345.6,-460.8`，
960×1280 画布下矩形有 3/4 在外面、中心压在画布左上角），却被当成**画布像素**用。
面板换算 `clamp((px-rc.dx)/dw, 0, 1)` → 往右/往下算出来超出 1 → 被夹住 → **纹丝不动**。

| 牛皮纸 · 960×1280 | 改前 | 改后 |
| --- | --- | --- |
| `photoRect` | `-345.6,-460.8,691.2,921.6` | `123.8,182.5,712.4,787.1` |
| `(.5,.5)` 画在画布 | `x = -1.1`（画布外）| `765, 891.5`（照片中心）|
| 往右拖（瞄 0.85） | 得到 **0.50**（没动）| 0.849 |
| 往下拖（瞄 0.85） | 得到 **0.30**（上一次的值）| 0.849 |
| 往左拖 / 往上拖 | 能动 | 能动 |

| # | 改法 | 为什么 |
| - | ---- | ---- |
| 1 | `rectThruM(rc, ctx.getTransform())`：四个角过一遍**绘制那一刻的 CTM** 取外接矩形 | `photoRect` 的契约是"画布像素"，就必须在**生产它的那个位置**换算过去，而不是让每个消费者各自去猜 |
| 2 | CTM 是单位矩阵时**原样返回**，不绕一圈 | 绕一圈会带进浮点噪声；下游有"严格 `>` 挑面积最大者"这种把噪声当信号的判据 |
| 3 | **不返回矩阵**，只返回矩形 | 返回矩阵 = 给"两套坐标系"开后门。代价是四个角最多差 `半宽×sinθ`（牛皮纸 1.6° → 1.6%）；**旋转下矩形中心不变**，`(.5,.5)` 仍然精确 |
| 4 | 再与**画框**求交 | cover 是"铺满再裁"，可见照片 = 铺出来的矩形 ∩ 画框。不求交则溢出的那一圈也算照片（明信片 ±158px）|
| 5 | 给 `cx` 加 `declare(x,y,w,h)`，双色调用它申报 | 双色不调 `c.paint`，矩形会退化成整块画布（锚点偏 **90px**）。`declare` 的语义就是"照片占这个框" |
| 6 | "面积最大者胜"加 `1e-6` 容差 | 九宫格 9 格面积**差在 1e-6 px² 以下**，严格 `>` 让"比前者大亿分之几"的格子胜出 —— 赢家由舍入决定 |

**这一轮最值得记的不是修法，是"怎么找到的"**

| 事项 | 差点写错的结论 | 真相 |
| - | ---- | ---- |
| 只有牛皮纸吗 | 静态扫 11 个模版的 `draw()`，只找到牛皮纸（唯一在 `paint` 前有变换的），差点就收工 | 静态扫描**漏了**双色（压根不调 `paint`，走自己的 `makeDuotone + drawImage`）和九宫格（平局）。改成"**11 个模版各自渲一遍、把 `photoRect` 打成一张表**"之后，一眼看到 3 个模版的矩形越界 + 5 个被 cover 溢出撑大 + 1 个退化 |
| 九宫格的死法 | 我记的是"三格面积**按位完全相等**，严格 `>` 让浮点噪声裁决平局" | 实测**连渲 12 次同值** —— 它在同一份代码里是**确定的**。真正的机制是面积**差在 1e-6 以下**，"比前者大亿分之几"就胜出，赢家由**舍入**决定。所以"连渲 N 次对比"这条检测器**抓不到它**；抓它的是 `_iso_rect.mjs`：**把 1e-6 容差拿掉，看赢家会不会换格子**（`352.6,184` → `623.7,455`）|
| 四处改动各自的靶子 | 一起改完只有一张"改前 vs 改后"的对照，说不清哪条修好了什么 | 又造了两份"只坏一处"的产物（`_tolon.html` 只去容差、`_nointer.html` 只去求交）+ `_before.html`（全去）做**隔离实验**。结论：牛皮纸只被 CTM 影响，明信片/霓虹/极简/电影感/宝丽来只被"求交"影响，九宫格只被容差影响，双色只被 `declare` 影响 —— **一处都没白改** |

**本轮验证**：**10 个测试套件 / 404 项检查全绿**
（`test_batch2` **90 → 101** 项：11 个模版矩形落在画布内 + 反面对照（修前的值过不了同一条判据）、
连渲 3 次逐位相同、双色不许退化成整块画布、九宫格锚点定值、"改前 8 个有毛病的模版一个都不复现"、
牛皮纸**真实鼠标四向拖拽**（往右/往下单列一条）、面板上那枚贴纸真的画在画布上；
其余 `test_book` 51 · `test_flow` 21 · `test_landing` 12 · `test_real` 27 · `test_skin` 33 ·
`test_note` 31 · `test_export` 30 · `test_auto` 48 · `test_mat` 50）。
**新断言必须能判"坏"**：把同一套 `test_batch2` 打回「改前」产物，**9 项失败**
（含往右/往下两条本身）—— 这一步不能省，否则"全绿"什么都不能证明。
产物 **1.122 MB（1,176,570 B）/ 余量 79.8 KB**（只改渲染坐标，photos 段未动）。
取证图：工作区 **`咔哒书-贴纸坐标系-改前改后.png`**（① 牛皮纸改前/改后各两格：成片 + 叠红框；
② 11 个模版整宽铺，红框越界的一眼可见）。
`_before.html` / `_tolon.html` / `_nointer.html` 三份"残缺产物"留在 `.workbuddy/tmp/` ——
它们是"改前对照 + 隔离实验"的一次性道具，**不是交付物**。

**通用教训**：
1. **一个值的「坐标系」要在它出生的地方说清楚。** `paintPhoto` 返回矩形，却没说"在哪个坐标系里" ——
   于是它有了两个消费者（都按画布像素理解）和一个**含义不同的生产者**（牛皮纸）。
   这种坑不报错、不崩，只让产品"手感不对"。
2. **要验的不变量要对「全部变体」成立，不是对你怀疑的那一个。** 11 个模版打一张表 +
   一条近乎废话的判据（"矩形必须落在画布内"），比"盯着牛皮纸看"多抓到 7 个。
3. **检测器自己也要有分辨力**（§9.1.4 的第三次应验，但失灵方式是新的一种）：
   不是阈值不对，是**量错了对象** —— "连渲 N 次"量的是确定性，而这里的病是"**确定的错值**"。
   验证办法：让检测器去跑**已知坏掉的产物**，坏的那个必须被报出来。

---

## 10. 我不建议做的五件事

1. **不建议替换现有视觉骨架** —— 那是唯一让产品不像廉价拼图的东西。
2. **不推荐做自由画笔**（涂鸦）—— 成本三倍，产出不如预置笔迹。
3. **不建议用 emoji 代替图标** —— 系统 emoji 在不同设备上长得不一样，会直接毁掉视觉一致性。
4. **不建议在第一批就做双人本** —— 它需要一个"半成品文件"的传递机制，闭环太长。
5. **不建议现在就做打印/冲印对接** —— 供应链问题，不是产品问题，且会带出大量客服成本。

---

## 11. 待你拍板

| # | 事项     | 选项                                                                |
| - | ------ | ----------------------------------------------------------------- |
| 1 | 手机端范围  | 路线 A 全量响应式 / **路线 B 简版流程（我建议）** / 本轮不做                            |
| 2 | 第一批动机  | **只做记录型 + 表达型（我建议）** / 四类都做                                       |
| 3 | 视频导出档位 | **先做逐帧 PNG 保底 + MediaRecorder 默认（我建议）** / 只做 MediaRecorder / 只做保底 |

拍板后我按第一批开工。
