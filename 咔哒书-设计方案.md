# 咔哒书 KADA · 3D 互动照片书 —— 设计方案

> 交付物：`咔哒书-3D互动照片书.html`（单文件，双击即用，零依赖、可离线运行）
> 参考形态：turn.js 杂志模式 / StPageFlip / create-photo-flipbook-ui / PDFlipbook

---

## 零、当前流程与状态模型（2026-09-17 修订）

用户实际要的流程是「**先给每张照片挑好模版，再勾选出要入册的，最后生成**」，
所以状态模型和首屏行为都按这个来。

### 0.1 三步流程

```
① 导入与规格          ② 模版美化              ③ 选片成书
导入 / 拖入 / Ctrl+V   点模版 → 记到「当前照片」   生成成片 → 3D 书 → 阅读
选画幅、调色           缩略图徽章显示各自模版      选片条微调收录 → 导出
勾选哪些照片入册       面板显示模版分配统计
```

- **首屏保持空白**，不自动载入示例照片。空状态给三个入口：
  「载入示例照片并一键成书」/「载入 28 张示例照片」/「导入我的照片」。
  第一个是从零到一本书最短的路径 —— 首屏最贵的成本是"我还看不到任何东西"。
- **勾选（入册）在左侧素材栏**，从步骤一就可用：缩略图左上角圆圈切换，
  未入册的缩略图降饱和变暗；工具条有「全选 / 全不选」。
- 顶部「生成成片」按钮直接写着会生成几张（`生成成片 · 14 张`），
  一张都没勾选时按钮禁用 —— 避免"点了没反应"。
- 没有素材时，底部胶片带用一张内置示例图渲染 11 套模版，
  而不是留一排空框。
- **首屏面板第一段是「一键氛围成书」**（见 §4.8）：想省事的人一次点击拿到一本书，
  想调的人再往下走（氛围 → 画幅 → 调色 → …）。三步流程没有变，只是前面多了一条捷径。

### 0.2 关键：模版是**每张照片各自一份**，不是全书一套

```js
// 照片对象
{ id, el, w, h, name, thumb,
  tpl:'film',      // ← 这张照片用哪套模版
  picked:true,     // ← 是否入册
  rot, zoom, ox, oy }

// state.tpl 的语义变了：
// 它不再是"全书的模版"，而是"胶片带上当前高亮的那一套"＝当前选中照片的模版。
// 切换选中照片时 syncTplFromSel() 把它同步成那张照片自己的模版，
// 所以胶片带高亮永远跟着你正在编辑的那张走。
```

- 点胶片带上的卡片 → `assignTpl(key,'sel')` **只改当前选中那张**，toast 明确说出
  改的是谁：「已把「胶片」记到「KADA 03」」。
- 需要统一时用「应用到全部」（胶片带右侧 + 面板里各一个入口）。
- `generate()` 只渲染 `picked` 为真的照片，且每张取 `p.tpl`：

```js
const list = state.photos.filter(p => p.picked);
const art  = renderCanvas(p, p.tpl || state.tpl, state.opts, LE);
```

### 0.3 持久化：本机保存 + 诚实降级

单文件离线运行，`localStorage` 在 `file://` 下并非所有浏览器都放行，
所以 `PERSIST` 模块**先探测再使用**，失败时静默降级并把状态如实写在面板里
（「本机已开启」/「当前不可用」），另给一个「清除已保存的偏好与分配」。

```js
{ tpl, theme,
  spec, adj, preset, opts, book,          // 设置
  assign: { 'KADA 01': {tpl:'film', picked:true}, ... } }   // 按照片名
```

- **分配按照片名（自然键）存，不按 `id`**：`id` 是每次运行自增生成的，
  重新导入就对不上；名字才稳定。示例照片名是确定的（`KADA 01..28`），
  用户重新导入同一批文件时文件名也一致，于是 `pushPhoto()` 里
  `PERSIST.assignFor(p.name)` 就能把上次的模版与入册状态接回来。
- 写入去抖 200ms（`save()` → `setTimeout(flush,200)`），
  在切模版、勾选、增删照片、改任何设置、切主题时调用。
- 照片本身**不存**（几十 MB 的 base64 塞不进 localStorage）——
  重新导入后模版会自动接回来，这一点在 UI 里说清楚，不装作存了。

### 0.4 步骤三与素材勾选的关系

步骤三的选片条是**成片级**的收录开关（在已生成的成片上再剔几张）。
如果素材那边又改了勾选，面板会提示「素材那边勾选了 N 张，当前成片是 M 张」
并给出「按当前勾选重新生成」，不会悄无声息地不一致。

---

## 一、总体思路

把需求拆成**三层各自独立、但共享同一份状态**的管线。任何一层的改动都会实时反映到另外两层，
所以"改一个画幅比例"，模版预览和整本书的重排是同时发生的。

```
素材层 → 模版层 → 成书层
 导入       Canvas 渲染     3D 装订与翻页
 规格       11 套模版       选片 · 书封 · 阅读
 调色       批量生成        导出 ZIP / 长图
```

**技术选型的关键决策：不引入任何第三方库。**
turn.js 与 StPageFlip 都是成熟的库，但引入它们会带来三个问题：需要 CDN（不能离线）、
难以和 Canvas 渲染管线共用状态、以及书页内容的自由度受限。
因此翻页引擎用 CSS 3D + Pointer Events 自己写，核心不到 200 行，换来完全可控的手感。

---

## 二、素材层：导入与规格

| 能力 | 说明 |
|---|---|
| 导入 | 拖拽到窗口任意位置、点击上传、**Ctrl+V 直接粘贴** |
| 示例 | 内置 28 张程序生成的示例照片 —— **首屏不自动载入**，点「示例照片」或空状态里的按钮才载入 |
| 入册 | 每张缩略图左上角的圆圈切换是否入册；未入册的降饱和变暗，不参与生成 |
| 模版 | 每张照片带自己的模版（徽章显示在缩略图上），详见第零节 |
| 画幅 | 原始 / 1:1 / 4:5 / 3:4 / 2:3 / 9:16 / 3:2 / 16:9 / A4 / 自定义宽高 |
| 分辨率 | 长边 600–4000 px 连续可调，按比例换算另一条边 |
| 适配 | 裁切填满（cover）／完整留白（contain）／**模糊铺底**（blur-fill，用原图模糊放大做背景再居中放原图） |
| 边框 | 白边宽度、圆角、投影、边框颜色，均为相对长边的百分比，换分辨率不失真 |
| 输出 | JPEG / PNG / WEBP + 画质滑块 |
| 调色 | 8 组预设 + 7 个可调参数（亮度/对比度/饱和度/冷暖/颗粒/暗角/褪色） |
| 单图操作 | 旋转 90°（缓存失效重算）、设为主预览、移除 |

调色实现走 `ctx.filter` 原生管线（性能好、无像素级循环），
颗粒与暗角分别用噪点贴图 `overlay` 混合与径向渐变叠加，都限制在照片裁剪区内。

---

## 三、模版层：11 套 Canvas 模版

每个模版是一个纯函数，接收统一上下文 `{ctx, W, H, photo, o, f, paint, plate, rr, grain}`，
其中 `paint(x,y,w,h,r,ov)` 负责把照片（含全部调色与适配逻辑）画进任意矩形，
`ov` 可覆盖缩放与偏移——九宫格模版靠这个给 9 个格子不同裁切。

| 模版 | 特征 |
|---|---|
| 宝丽来 | 厚白底 + 手写体标题 + 投影 |
| 胶片 | 上下齿孔 + 橙色片号 + 竖排 KODAK 400 |
| 杂志 | 满版出血 + 压暗渐变 + 大标题 + 期号 + 条码 |
| 牛皮纸 | 纸张纤维 + 双胶带 + 圆邮戳 + 手写日期 |
| 霓虹 | 点阵底 + 发光双描边 + 荧光标题 |
| 极简 | 大留白 + 0.5px 细线 + 小字 |
| 明信片 | 上分割图 + 地址线 + 取消戳 |
| 双色 | **像素级双色调映射**（读取 ImageData，亮度映射到双色渐变）+ screen 混合巨字 |
| 九宫格 | 3×3 拼贴，每格独立缩放偏移 + 白缝 |
| 宽银幕 | 上下黑边 + 时间码 + 字幕条 |
| 章节页 | 无图 / 极淡底图 + 大编号 + 分隔线，用于插入章节 |

所有模版共用一组参数：主标题、副标题、角标、纸张色、墨色、强调色、字体风格（无衬线/衬线/等宽/手写）。
标题类文字用 `fitFont()` 自动收缩字号以避免溢出。

底部"模版胶片带"会**用当前选中的那张照片**实时渲染 11 张缩略图，所见即所得 ——
看中的直接点，就记到这张照片上（详见第零节）。还没有素材时用内置示例图演示。
拖动平移 · 滚轮横向滚动 · Ctrl+滚轮缩放卡片尺寸。

---

## 四、成书层：3D 选片条 + 翻页引擎

### 4.1 3D 互动选片条
Coverflow 形态的立体照片条：拖动旋转、松手吸附到最近一张、点击切换是否收录。
未收录的照片自动降饱和并变暗，收录的显示琥珀色勾选。
第 N 张的变换为 `translateX(d×112) translateZ(-|d|×74) rotateY(-d×30°) scale(...)`，
`d` 是相对当前焦点的连续偏移量，所以拖动过程是平滑的而非跳变。

### 4.2 真实纸张物理模型（Canvas 2D 自研，非 CSS 3D）

> 早期版本用 CSS 3D（`rotateY` + `backface-visibility`）翻转，实测做不出好看的纸张：
> 中间态会塌成一条黑边 / 出现半透明灰带 / 后半程整张纸悬空。已整体废弃。

现在的实现是 **Canvas 2D 的「剥离式卷曲」（peeling curl）** 几何模型，
完整推导、踩过的六个错误模型、以及全部像素级验证方法见技能
`~/.workbuddy/skills/canvas-page-flip/SKILL.md`。要点：

- 切线角剖面 `A(t)=π·A1·[(1-λ)+λ·(t/tc)^0.7]`：自由边领先、书脊滞后，`q→1` 时全页平整落地
- 沿纸面积分 `X=∫cosA du`、`h=∫sinA du`，`h` 以落页点归零（否则后半程纸会悬空飘着）
- 投影 `sx = spineX ± X`、`sy = cy ∓ h·KZ`，所以纸真的"立起来"且上下边缘是曲线
- 逐条带 `drawImage` 把源图压进屏幕条带，**外扩量按条宽成比例**（写死像素会导致中段图被平均成纸色）
- 连续着色：可见面法线点乘光源 + 平放端 `winOf` 窗（否则落地瞬间亮度跳一档）
- 书脊/书沟阴影取两侧纸存在度的 `min`（只有封面时不存在书沟，画了就是"左边一道痕迹"）

#### 4.2.1 两种材质：普通纸 vs 硬纸板（第五轮，对标 StPageFlip）

前一版把所有页面当成同一种纸，于是**封面也像一张纸那样卷**。翻页时 `λ` 峰值是 0.94，
意味着书脊一侧滞后 94% —— 纸被卷成一卷，中段几乎看不到纸面、只剩一道弯曲，
读起来发软，像橡胶而不是纸。对标 StPageFlip 之后重做：

| | 普通内页 | 硬纸板（封面 / 封底 / 环衬） |
|---|---|---|
| `λ` 峰值 | **0.34**，且有拖拽时按 `bowBoost` 加到 ~0.6（甩得越快、被风鼓得越明显） | **0.10** —— 基本是刚体平板 |
| 运动 | 剥离式卷曲：书脊处一道 bow，自由边领先抬起 | 绕书脊竖轴旋转，像一扇门：投影宽度 `W·cos(πq)` 由满→零→满 |
| 投影系数 `KZ` | `0.46·(1−0.30q)`（纸折起来，自由边真的升高） | **`0.12`**（刚体绕竖轴转，自由边几乎不升高） |
| 投影阴影 | 柔、宽（`0.34W + 0.058W·lift`，10 层同形微偏移） | 更实、更短（`0.46W`，`0.024W + 0.042W·lift`） |
| 板厚度 | — | 厚度面只在侧立时露出：屏幕上宽度 = `板厚 × |sin(πq)|`，两端自然归零 |

板厚度这一条是「精装 vs 平装」在翻页过程中的全部证据，成本只有一个 `fillRect`。
材质由 `BookView.kinds[]` 记录（`setPages` 时从页对象带过来），
`sheetHard(i)` 只要这一叠的任一面是 `cover/back/endpaper` 就算硬页。

#### 4.2.2 时长与缓动

- 缓动从 `1-(1-t)³` 换成 `1-(1-t)^4.2`：3 次方到末尾还剩明显速度，「啪」地停住；
  4 次方以上变成「一口气翻过去，然后慢慢压到纸面上」，重量感就出来了。
  硬纸板反过来用 3.0 —— 刚体靠惯性匀速转，尾巴太长就成了慢动作。
- 面板新增**翻页手感**三档：利落 520ms / 标准 820ms / 舒缓 1180ms
  （StPageFlip 的 `flippingTime` 默认 1000ms，标准档取 820ms）。
  只作用于「点击左右半边 / 按钮 / 自动翻页」这类没有速度信息的一翻；
  拖拽松手那一翻的时长仍由手速决定。
- 这一项只是动画参数，**不重排书页**（那是几十张 canvas 的重绘）——
  分段控件在派发 `book.speed` 时要显式跳过 `renderStage()`。

#### 4.2.3 悬停折角

鼠标停在外下角时掀起一角：翻开的是纸的**背面**（偏亮、没有图），
露出的是下面那一页（偏暗、靠近折痕更暗）。判定必须同时满足「贴近书口那一侧的竖直边」
和「页面下缘」，否则鼠标随便划过去都在掀角，反而更像故障。
`cornerA` 在 `step()` 里以 0.20 的系数淡入淡出，翻页/拖拽进行中强制归零。
有这一下，页面才从「一张图」变成「一叠纸」。

#### 4.2.4 折角锚在「书口边」而不是「书沟」（第九轮）

用户反馈：**「图书翻页有问题吧，怎么会在那个位置上出现呢」** —— 悬停左页左下角时，
翘起的那一角出现在**书的中间缝上**，像是右页的内下角凭空折了一下。

根因是一行坐标。折角锚点写成了：

```js
const px = right ? this.spineX + pw : this.spineX;   // ← 左页这半边少了 - pw
```

而 `spineX` 在**两种模式下含义不同**：合着书时它是"当前页的左边"，
**跨页时它是书沟**（左页在 `spineX - pw`，右页在 `spineX`）。
左页折角要的是左页的**书口边**（外侧那条边）= `spineX - pw`，
写成 `spineX` 就正好落在书沟上 —— 右页那半边因为本来就是 `+pw`，所以一直是对的，
于是症状表现为"只有左页会错"，看起来更像随机故障。

```js
const px = right ? this.spineX + pw : this.spineX - pw;   // 两侧都取各自的书口边
```

**取证方式**：不靠肉眼找白片（那片是渐变，阈值挑不出来），改成
「同一帧 **开/关折角** 两张图的像素差」—— 差的 bbox 就是折角真正落在哪。
改前悬停左页落点 `x∈[463,515]`（书沟 463 上），改后 `x∈[95,150]`（左页书口边 99 上）。
这条已固化为 `test_landing.mjs` 的 ④，并带**反向对照**：
把锚点还回书沟，同一判据必须判不合格（否则这条断言是空转的）。

> 教训：`spineX` 是个**语义随模式漂移**的变量（书沟／页左）。
> 凡是要"某一页的某条边"的地方，都该显式写成 `spineX ± pw`，
> 不要依赖 `spineX` 当前是什么。

### 4.3 页面排版与文字

#### 4.3.1 书页画面：模版成品（默认）或干净照片

`generate()` 现在同时产出两张画布：

```js
const mo    = optsFor(p);                            // 模版文字按照片解开（见 4.3.3）
const art   = renderCanvas(p, p.tpl, mo, LE);        // 模版成品：宝丽来白框 / 杂志大字 / 胶带……
const plain = renderPlain(p, LE);                    // 干净照片：只走调色管线，按照片原始比例
```

`state.book.art` 决定内容页取哪张，**默认 `'tpl'`**：

| 取值 | 书页画面 | 适合 |
|---|---|---|
| `tpl`（默认） | 模版成品，当一张**装裱好的印片**放进版心（四周留纸边 + 投影） | 想看见自己逐张挑的模版 |
| `plain` | 干净照片，按照片原始比例排 | 一本正经的摄影集 |

**这里有个前一轮的教训。** 第五轮为了「好看」，让书页一律用 `plain`，结果是用户
逐张分配的模版**在成书里完全看不见** —— 模版功能形同虚设。用户的反馈很准：
「我选择的模版不会显示在成书那，那我这个模版功能做了干嘛呢」。

真正的丑源**不是用了模版**，而是模版那句**全局同一行文字**：`state.opts.title` 原本是
一个常量 `'今日份'`，于是 28 页整整齐齐地印了 28 遍，下面再叠一行书本图注 ——
**同一种信息出现两次**。修法是让文字按照片取值（4.3.3 的占位符），而不是把模版扔掉。

配套的两条规则：

- **模版自带题字时，书页不再画图注**（`plateHasText()` / `capWillDraw()`）。
  既然画面里已经有「KADA 12」，下面就不该再写一遍。
- 不画图注时，照片箱体的下沿从「图注上方」放开到版心底 ——
  否则「留给图注的位」会变成页面下方一条没人用的空洞（`mat` / `two` / `sticker` 都按这条调）。

封面 / 扉页 / 环衬**仍然走 `artOf()`（干净照片）**：那几页的排版是整套设计的，
再叠一层模版外壳只会打架。

#### 4.3.2 四套版式重做

| 版式 | 做法 |
|---|---|
| 满版出血 | 照片铺满整页 + **书沟一侧渐暗**（`0.15W`），并在天地各压一条极淡的边；跨页打开时两页才像同一本书而不是断开的 |
| 居中留白 | **图注锚在固定基线**：细线落在 `capY = H − PB − 0.048W`（实测恒为页高的 86%），页脚留白每页都是 14%。照片在「上边距 → 图注上方」这个箱体里居中，于是竖构图照片几乎填满箱体、图注紧贴其下；横构图照片居中浮起，图注**仍落在同一条基线上**，下方的空白被拆成「照片下方的安静留白」+「正常页脚」，不再是一大块没排完的空 |
| 双图并置 | 两张各自保持比例，**各自对在自己那张图下面**写图注，不再是两行名字用 `·` 串在页面正中 |
| 贴纸手账 | 照片旋转 −0.032 rad + 白卡纸 + 两条和纸胶带（一上一下压住对角），图注用手写体 |

- **「留白箱体的下沿」取决于这一页到底画不画图注**（`capWillDraw()`）：
  画 → 下沿收在细线上方 `0.062W`；不画（模版成品自带题字）→ 一直放到版心底 `0.024W`。
  少了这一条，`{name}` 这种「自带题字」的页面下方会留下一条谁也没用的空洞，
  又回到「没排完」的观感。`two` 与 `sticker` 同理（`sticker` 没有图注时把画面
  略微放大并下移，胶带跟着一起平移）。

- 图注一律改成「短细线 + 衬线小字 + 0.14em 字距」的摄影集做法；
  `ctx.letterSpacing` 不是所有浏览器都有，自己写了 `tracked()` 逐字符绘制，
  顺便拿到整行宽度好做居中/右对齐。
- **图注居中到版心、而不贴照片下缘**：图注跟的是「版心基线」，不是「这张照片」。
  这是让一本 28 页的书读起来像一本书、而不是 28 张各自排版的页的关键 ——
  否则横构图照片的图注会浮在页面中间，上下各空一大块。
  双图并置版式里两张各有各的图注，但仍共用同一条基线（`H-PB-0.045W`）。
- **页码 folio 落到书口一侧**：跨页时 canvas 索引偶数在右页、奇数在左页
  （见 `BookView.restFor`），所以 `buildBookPages` 在 push 每一页时按当前奇偶算出
  `side='l'|'r'` 传给 `renderContent`。单页阅读时居中。
- 封面 / 封底 / 扉页 / 环衬一起重做：封面走**眉题→主标题→细线→副题**的四段式中轴排版，
  布纹改用两个方向的斜织纹交叠；封底做版权页式的收尾（书名 → END OF VOLUME → 条形码白卡 → 出版行）。
- 封面与扉页的主图用 **contain 而非 cover**：相框是横的，竖构图照片一裁就只剩中间一条脸。
  先按比例缩到放得下的最大尺寸、再在预留区里居中，整张照片都在，外圈留一圈白卡纸。

#### 4.3.3 模版文字按照片取：占位符

`state.opts.title / sub / corner` 支持占位符，出片时**按照片**解开：

| 占位符 | 含义 | 出厂默认 |
|---|---|---|
| `{name}` | 照片名（`KADA 12`） | `title` = `{name}` |
| `{n}` / `{nn}` | 序号 / 两位序号 | `corner` = `NO.{n}` |
| `{total}` | 入册张数 | `sub` = `{n} / {total}` |
| `{tpl}` | 模版名（宝丽来 / 胶片…） | — |

`optsFor(photo)` 生成这一张成片真正要用的 opts，`generate()`、预览区、胶片带三处都走它 ——
**预览里看到的那行字，就是成片与成书里会有的那行字**。

不认得的占位符原样留下（方便看出自己写错了）；面板里给出可用清单，
并在有照片时直接显示一行「当前选中照片的示例」。

**老存档迁移**：`PERSIST.restore()` 里做定向升级 —— 只把「原封不动等于旧默认值」的字段
（`今日份` / `2026 · LUMEN` / `NO.01`）换成新默认；用户自己改过的文字一律不动。
不做这一步的话，已经用过上一版的人升级上来，看到的仍然是 28 页重复同一句话，
**等于这个修复对他不可见**。

#### 4.3.4 封面主图自动挑一张

「取第一张」几乎必然撞上一张虚焦 / 逆光 / 构图失败的照片 ——
示例素材的第 1 张就是一张大虚焦脸。所以 `coverScore()` 在一张 48px 小图上算
「对比度 + 色彩丰富度 + 相邻行差（清晰度）」，再对过曝 / 过暗扣分：

```
score = sd·1.15 + rich/n·0.55 + sharp/n·0.55 − max(0, |mean−128|−58)/58 · 30
```

28 张的总成本可以忽略，但封面立刻从「随手一张」变成「挑过的」。
面板里显示当前选中哪张 + 「换一张」按钮按打分顺序轮换；
`state.book.coverIdx` 与其余设置一起持久化。打分结果缓存，`generate()` 时作废。

其余：翻页形态跨页对开 / 单页阅读；图注来源照片名 / 模版标题 / 不显示；
书封可改书名、副标题、署名、**书脊竖排文字**、封面底色、内页底色、内页墨色；
墨色按封面色亮度自动切换深浅以保证对比度。

### 4.4 交互
拖拽翻页 · 点击左右半边翻页 · ← → 键盘翻页 · 底部页码缩略图跳转 · 自动翻页 ·
缩略图导航 · Esc 关闭 · 全屏沉浸阅读模式（复用同一个书籍实例，不重建 DOM）

### 4.5 导出
| 方式 | 说明 |
|---|---|
| 导出全部成片（ZIP） | **手写 ZIP 写入器**（store 模式 + CRC32），无依赖，已用 Python `zipfile` 校验通过；文件名带尺寸与预设标签，方便直接分发 |
| 导出竖版分享长图 | 1080 宽竖版：顶部书名 + 日期，正文是去掉环衬/空白页的书页，底部留裁切安全区；超过 16000px 会等比缩小，不会出一张打不开的图 |
| 导出书页预览长图 | 所有书页按 4 列平铺成一张联系表 PNG |
| 下载当前成片 | 按设定格式与画质导出 |

页数为奇数时自动补空白页，保证封底永远落在纸张的背面。

**发布尺寸预设（第七轮）**：小红书竖图 `1080×1440` · 故事全屏 `1080×1920` · 方图 `1080×1080` · 明信片 `1080×1620`。
预设改的是**成片本身的画幅**，不是导出时套一层白边 —— 后者会得到"一张带白边的方图"，
还得再裁一次，不算"能直接发"。书页比例（`book.ratio`）与成片画幅（`spec.ratio`）刻意解耦，
否则选个 9:16 预设会把整本书压成手机屏。

### 4.6 氛围层：7 套皮肤（第七轮）
同一套骨架，多种气质。皮肤**只给 token，不复制模版**：

- 11 套模版打上 `tone`（`light` / `dark` / `photo` / `kraft`），纸色从当前 skin 派生（`optsFor()`）。
  11 × 7 = 77 份配置的方案等于"改一个色号要改 11 处"，必然烂掉。
- 唯一的写入点是 `applySkin()`：它必须**同时**改 `state.opts`（模版印片）与 `state.book`（书页 / 书封）。
  只改一边就会出现"书页变粉了、印片还是米色"的割裂，比不换还难看。
- 亮色主题下工作台强调色走另一档色值（`ui.light`），一套色打天下在浅色底上对比度不够。
- 7 套 = 原味（出厂默认，观感与加皮肤之前**逐字段一致**，已用脚本比对过）+ 奶油日记 / 蜜桃气泡 / 雾紫梦境 / 薄荷苏打 / 夜樱（深底书）/ 法式午后。
- 浅色皮肤的**书页纸色比模版纸色深一档**：一开始两者几乎同色，像素采样显示 7 套皮肤肉眼分不出来；现在 6 套有色的皮肤都保证 `lum(bookPaper) − lum(tplPaper) ≤ −6`。

#### 4.6.1 材料/质感层：让"氛围"不只是换色（第七轮修订）

用户原话：**「就是感觉单单纯纯的换了个色，没有任何质感，比如书面质感，写实真实质感」**。
这条反馈意味着"氛围 = 一套配色"这个模型本身不够 —— 换颜色谁都会，**材料**才是"书"的身份。

于是在皮肤之下加了一层**材料**（`MATSETS`，7 套配方），每套配方是一个四元组：

| 字段 | 含义 | 取值 |
| --- | --- | --- |
| `cover` / `page` | 封面料 / 内页料的贴图种类 | `linen` `canvas` `kraft` `laid` `silk` `leather` `coated` |
| `emboss` | 封面题字工艺 | `deboss` 压凹 / `foil` 烫金 / `plain` 素题 |
| `tex` `sheen` `lit` | 纹理强度 / 涂层光泽 / 光照强度 | 倍率 |

- **贴图是过程生成的、确定性的**（`matTile(kind,seed)`）：用 xorshift PRNG 在 256×256 的 tile 上画织纹 / 长纤维 / 帘纹 / 软斑 / 颗粒。
  **必须确定性** —— 同一 `kind+seed` 每次都要一模一样，否则每次重排书页纹理都会"闪"一下。
- **必须可无缝平铺**：所有特征都通过 `at()` 在 ±256 的四个方向各补画一遍。这一条踩过坑：
  丝绸最初用 4 条对角渐变铺满整张 tile，渐变在 tile 边界不连续，**880px 的书页会平铺它三四次，于是每 256px 一条竖直接缝**（实测列均值跳变 26.8/255，是相邻列的 5.4 倍）。
  现在改成**整周期正弦**（波数取整数，周期正好等于 tile 边长），天然接得上。判据也从"相邻列均值倍率"改成"**接缝跳变 vs 贴图内部最大跳变**"——前者会把帘纹纸每 4 行的正常大跳误判成接缝。
- **三层渲染**：① 底色（皮肤给的色号）② 材料 tile（`paperGround` / `boardGround`）③ 光照（上缘受光/下缘背光、书沟侧阴影、纸边厚度 `pageEdge`）。
  封面另加漫射光 + 四角渐暗 + 板缘厚度。所以"材料"是叠在色号**之上**的一层，不是替代色号 —— 换材料不会改变书的颜色。
- **题字工艺**（`pressTreat`）是"书"和"一张印了字的纸"最大的差别：压凹走"暗影 + 亮边 + 面色"（光从左上来的物理关系），烫金走皮肤给的 `foil` 色做的多段渐变。
  暗色封面上的效果最明显（见取证图 `Z-题字工艺.png`）。
- **封面 tile 叠两遍**（第二遍 1.7× + 轻微旋转）把平铺周期搅掉 —— 但**只对粗料做**。
  细而匀的料（丝绸柔光 / 涂布纸）叠两遍只会互相干涉出摩尔纹（暗封面上是一眼可见的菱形格）。
- 氛围给 3 种材料（`SKINS[*].mats`，第一个是默认），面板里是**真材料小样**（92×30 canvas，直接调 `boardGround` 画）——
  画一个近似色块会让用户没法预判换上去是什么样。
- **材料是"装帧层"，刻意不烧进成片**：换材料只重排书页（`scheduleBook`），不重新生成，也不会把成片判为过期。
  它留下的痕迹在**书页/书封**上（由像素差直接验证），而不是在每一张照片里 —— 让用户为换一次装帧重等 28 张渲染是错的。

### 4.7 文案层：一句话 + 心情标签（第七轮）
她缺的常常不是照片，是"配什么字"。

- 6 组情绪（治愈 / 元气 / 甜 / 文艺 / 搞怪 / 正经）× 7 条，每条都支持占位符，
  于是同一句文案套在 28 张照片上会各自不同，不会 8 页重复一句话。文案都控制在 14 字以内（模版印片侧的标题行没有自动缩排）。
- 图注默认值从 `name` 改成 `note`（没写文案时退回照片名，所以对老用户**观感完全没变**）。
  这一条是刻意的：如果默认是"照片名"，用户写了半天文案书上一个字都不显示，功能就白做了。
- 改文案**不重出成片**，只重排书页 —— 否则改一个字要等 28 张重新渲染，没人愿意写。
- 面板里的"挑一句"会自动跳到下一张；「套给全部」以"当前这张"为准，空了就退回"刚挑的那句"（否则挑完自动跳走会让按钮莫名其妙地拒绝执行）。

### 4.8 一键氛围成书（第七轮）
一条给"表达型 / 记录型"用户的捷径：他们卡住的不是不会点，而是不愿意为 28 张照片做 28 次决策。

一次点击做完四件事：**套氛围 → 按推荐序列铺模版 → 自动挑封面（复用 `4.3.4` 的 `coverScore`）→ 直接生成**。

- 每套氛围带一组 `tpls` 推荐序列（4 套模版，7 套氛围合起来覆盖全部 11 套）与一个 `rec.layout` 推荐版式。
- 「换一版」换的是**这一版的排法**：序列起点 +1、封面顺延一张、版式在推荐的两档之间轮换、**材料在氛围推荐的 3 种之间轮换** ——
  但不换氛围（那是用户选的）、不换照片。
  最初「换一版」只挪模版顺序，用起来很单一（用户原话：**「这个换一版功能用起来也很单一」**）；
  现在它换的是**整本书的装帧**（材料 + 版式 + 模版排法 + 封面），一次点击能看出"这是另一本书"，而不是"同一本书换了个顺序"。
- 它**没有任何后门**，只是替用户把面板上现有的动作点了一遍，所以点完照样能逐张改模版、换封面、换氛围。
  「逐张可控」这个前提没有被破坏，只是把"从 0 到有一本书"的成本从几十次点击压到一次。
- ⚠ 铺模版的顺序必须和 `generate()` 里那个 `filter(picked)` 的顺序**完全一致**，
  否则"第 3 张"在成片里对不上 `generated[3]`，逐张改模版时会改错人。
- 一张都没勾选时默认"全部做进去"，但按钮文案会先说明这件事；`pickAll:false` 时明确拒绝而不是默默照做。

### 4.9 字体角色：图注是正文，标题是题字（第八轮）
用户反馈：**「显示的字体太板正了，不太好看还跟右边显示的字体不一样」**。

原来只有一个 `serif` 角色，图注直接借它 —— 而 `serif` 的中文是**宋体**。宋体在屏幕上是
"横细竖粗 + 三角收笔"，小字号下公文/报纸气很重，正是"板正"；同一行里拉丁走 Georgia
（轻、圆、有字距），一个跨页左边宋体右边 Georgia，就成了"跟右边不一样"。

- **字族按"用途"分角色**，不再一个 `serif` 通吃：
  `serif` = 题字（封面 / 书名页 / 版权页 / 导出页眉），要端正；
  `cap` = 正文（书页那句话），要温度和手写气；`hand` = 手写体（贴纸版式）；
  `sans` = 界面；`mono` = 数字。
- 图注的中文字族首选**楷体**（Windows `KaiTi` / macOS `Kaiti SC`／`STKaiti`），
  拉丁仍然 Georgia —— 用户认可右边那行，就不动它。楷体比宋体轻（笔画覆盖率低约一成），
  所以图注墨色从 `.84` 抬到 `.90`，观感落在原来那个"轻"上，而不是变灰。

**这里有个必须写下来的坑：单文件 / 离线 / `file://` 工具里，中文字族只能从系统里找，
而浏览器遇到不存在的字族不报错、直接静默降级。**
所以"我写的是宋体/楷体"和"用户看到什么"会脱钩 —— 同一份文件、同一行代码，
开发机上落到宋体、换个环境就落到黑体（用户截图里那句就是黑体，与界面同款）。
于是开机时用**位图探测**把候选逐个验一遍：同一个"中文 + 拉丁"样本，
分别用「这个字族」和「一个绝对不存在的字族」各画一遍，像素有差才说明它真的在；
挑第一个确实装了的。样本要带拉丁，是因为中文字族的 CJK 有可能和系统默认中文一模一样
（这时只看中文会把装了字的判成没装），而各家中文字族的拉丁字形差别明显。
一个都验不出来时**不退给 `serif` 去瞎猜**，而是明确退到界面同一套黑体 ——
至少"书里的字"和"界面上的字"是一套，不是某种意外。

- **面板把实际用到的图注字族写出来**（"图注字体 楷体"）。
  不写出来的话，"系统里没这个字族"这件事对用户和对开发者都是隐形的 ——
  这一条就是这一轮排查时最缺的那个信息。
- 这件事有客观判据（位图哈希），所以进了回归：见 `6.2`。

---

### 4.10 品牌：名字、图标、字标（第十轮）

原品牌是**光匣 LUMEN**（暗房气质、文气），现改为**咔哒书 / KADA**。
「咔哒」是拟声词 —— 快门那一下，也是合上书那一下；两个意思正好压在这个产品的两半上。
气质从"文气"转向"俏皮"，也更贴合原本的目标用户。

**图标**：沿用原来的形状资产（**圆角方块 + 琥珀金**），把里面的「匣」字换成一个真正的图形 ——
**一本摊开的书，从方块里挖空出来**。

| 决定 | 为什么 |
| --- | --- |
| 保留圆角方块的形状 | 这是原来唯一被用户天天看到的形状资产，换掉等于从零开始认品牌 |
| 图形走**挖空（evenodd）**而不是叠色 | 挖空处是透明的 → 深底上是"深书"、浅底上是"白书"、换皮肤时是"皮肤色的书"。**一个文件适配所有底**，不需要出深浅两版 |
| 纯矢量、不依赖字体 | 零依赖单文件工具里字族全凭系统（见 §4.9 的坑）。方块里的那个字一旦系统没有，图标就废了 —— 图形化顺手把这个风险也一起解决了 |
| 图形不追求"一眼看出是相机" | 试过「圆环+书」「六叶光圈」「书即光圈」「右页掀起」四版：16px 下要么糊成一团、要么读成罗盘。**先保证小尺寸认得出来，再谈巧** |

几何（48 网格）：外框 `41.6×41.6`、圆角 `12.9`（圆角/边长 ≈ 0.31，接近系统级 App 图标的圆润度）；
书 `31.0×20.3`，占外框 74%，中缝 `2.1`，页外缘外鼓 `1.1`（不做成直棱 —— 一点点外鼓，纸才有肉感）。

**踩到的两个坑**（都值得写下来）：

1. **"绕原点缩放"**：书的路径用 `kk(cx + v, s)` 生成，等于把中心坐标 `24` 也乘了一遍 `s`
   —— 书被整体推到右下角，而且因为幅度不大，看图时**很容易以为是"刻意的重心偏移"**。
   正确写法是只缩放"相对中心的偏移"：`sx(v) = cx + v*s`。凡是要"绕某点缩放"，都要显式写成
   `中心 + 偏移*比例`。
2. **掩膜没生效**：先用 `<mask>`（白方块挖黑书）做镂空，结果整块实心 ——
   排查成本远高于换做法。改用**单路径 `fill-rule="evenodd"`**（外框 + 书的两条子路径）
   一次搞定，且语义更直白："外框减书"。**能用 evenodd 表达就不要上 mask。**
   （另外踩到：CSS `.mk{fill:currentColor}` 会把形状上的 `fill="none"` 一起覆盖成实心 ——
   环变成了实心圆盘。要显式控制就写在属性上，别用 CSS 兜。）

**改名落地**：`.workbuddy/tmp/src/` 里所有用户可见文字（标题、默认书名/署名/书脊、
页眉页脚小字、导出文件名与压缩包内说明、沉浸阅读标题、示例照片名 `KADA 01..28`、
环衬上那枚印从「匣」改成「咔」）。品牌名收成常量 `BRAND` / `OLD_BRAND` 两份 ——
默认值与"改名迁移"共用同一处，否则很容易出现"默认值改了、迁移里还写着旧名"。

**两件故意不动**：
- 存档 key 仍是 **`lumen.prefs.v1`**（不对用户显示，改掉会让已经做过书的人丢存档）；
- JS 命名空间仍是 **`window.LUMEN`**（另加 `window.KADA` 别名），只改用户看得见的文字 ——
  改内部命名要把整套回归都跟着改，收益是零。

**老存档迁移**：书名/署名/书脊若**原样等于旧默认值**（`光匣` / `LUMEN STUDIO` / `LUMEN · 2026`）
就换成新的；用户自己改过的一律不动。见 `6.4`。

---

## 五、界面设计（创意点）

**视觉基调：暗房（Darkroom）**。照片工作台天然适合深色环境，主色 `#0b0c0f`，
强调色用**琥珀金 `#f0a63c`**（暗房安全灯的颜色），配合 4% 白玻璃层与极细描边。
右上角可一键切换浅色主题。

> 第七轮起强调色**跟着氛围皮肤走**（`applySkin` 写 `--accent/--accent-ink/--glow/--r/--r2/--r3`），
> 亮色主题另取一档更深的色值保证对比度；琥珀金从"唯一的强调色"变成"原味这套氛围的强调色"。
> 视觉骨架（网格底纹、噪波层、投影、中缝阴影）没有动 —— 那是唯一让产品不像廉价拼图的东西。

- 顶部为三段式步骤条，当前步骤高亮琥珀环
- 左侧素材栏使用错峰入场动画（`animation-delay` 逐张递增）
- 中间舞台垫了 26px 网格底纹 + 径向光晕，预览画布常态微倾 1.6°/2.4°，
  鼠标悬停时回正——一个很轻但很有质感的交互
- 右下角噪点噪波层 `mix-blend-mode: overlay`，消除纯色塑料感
- 底部模版胶片带悬停上浮，选中态双层发光描边
- 成书页为深色的全屏阅读器，书页带真实投影与中缝渐变阴影

---

## 六、已验证项

用无头 Chromium 实际执行，全部通过（脚本都在 `.workbuddy/tmp/`）：

| 脚本 | 覆盖 | 结果 |
|---|---|---|
| `test_book.mjs` | 书本观感 + 翻页手感 + 书页画面（含成片新鲜度 / 改模版判过期）/ **改名迁移（旧默认值换新名、用户自己改过的不动、opts.title 不许被碰）** | 44/44 |
| `test_flow.mjs` | 首屏空状态 / 载入示例 / **逐张分配模版** / 应用到全部 / 勾选入册 / 只生成入册的且各用自己模版 / **刷新后分配与勾选被恢复** | 21/21 |
| `test_real.mjs` | 全部交互走 CDP 原生鼠标事件：11 张卡片逐张真实点击、Ctrl+滚轮缩放胶片、拖拽+惯性、手抖 3px 仍算点击、选片条命中自证、箭头、键盘 | 27/27 |
| `test_landing.mjs` | 封面态无书沟阴影（Δ=1.2）/ 跨页态书沟仍在（Δ=−40.32）/ 翻页落地亮度连续（Δ<1.5）/ 预览区无固定黑印 / **悬停折角落点（两侧都在书口边，含反向对照）** | 15/15 |
| `test_skin.mjs`（第七轮） | 原味默认观感不变 / applySkin 同时写 opts+book / tone 分派（深色模版不被刷白、牛皮纸走 tplKraft）/ 换皮肤后像素真的有差异 / 书页过期判定 / 持久化 / 亮暗主题两档强调色 / 首屏卡片 / 真实点击路径 / 用户手改优先与「跟随氛围」重置 / 书页纸色确实比模版纸色深 | 33/33 |
| `test_note.mjs`（第七轮 · 第八轮改） | 图注默认 `note` 且没写时退回照片名 / `{note}{mood}{date}` 解开 / 每张各不相同 / 图注序号与模版序号一致（生成项 vs 照片）/ 改文案不算过期 / 超长自动缩字与截断 / 面板渲染 + 真实点击 + 自动跳下一张 / 套给全部 / 按文件名持久化 / `cap='name'` 老存档迁移 / **图注真的画出来了（同页"有文案 vs 无文案"带内像素差，四个版式）** / **字族探测（含反向对照）/ 图注中文不是黑体 / 拉丁仍是 Georgia / 图注与标题是两个角色 / 面板写出实际字族** | 31/31 |
| `test_export.mjs`（第七轮） | 4 档预设声明像素与实际一致 / 选预设真的改成片画幅（不是导出套白边）/ 改尺寸判过期 / 书页比例与成片画幅解耦 / 分享长图竖版 1080 宽 + 顶部书名 + 底部安全区 / 去掉环衬与空白页 / 超 16000px 等比缩小 / 真实按钮可用 | 21/21 |
| `test_auto.mjs`（第七轮） | 首屏卡片与按钮禁用态 / 勾选数写在按钮上 / autoBook 四件事逐条核对 / **成片顺序与分配一一对应（`generated[i]` ↔ 第 i 张入册照片）** / 未勾选时默认全做、`pickAll:false` 明确拒绝 / 换一版换了什么与没换什么 / 一键之后手改模版仍生效 / 真实点击一键成书与换一版 / 推荐序列自洽（无非法名、无重复、覆盖全部 11 套）/ **说明段落必须换行不被截断（class 撞车守卫）** | 48/48 |
| `test_mat.mjs`（第七轮修订 · 新增） | **材质/质感层**：配方表自洽 / 每套氛围 ≥2 种材料且默认料正确 / 不搭才换、搭就保留 / 贴图确定性（同 seed 逐像素一致）/ 换 seed 变 / 7 种互不雷同 / 粗料方差与细料峰谷差 / 叠加后不改变底色明度 / **左右与上下都可无缝平铺（含反向对照：整幅渐变会被判为"有接缝"×114）** / 同底色下换材料像素必变 / 题字三种工艺像素互不相同 / 选择器小样是真材料且互不相同 / **真实点击换材料** / 换材料只重排不重生成 / 换一版连材料一起换且不换氛围照片 / 存档与老存档迁移 | 50/50 |
| `test_all.py` | 生成 28 张 → 34 页 17 张纸、键盘翻页、拖拽、回翻、跳页、单页、阅读器、主题 | errors=0 |
| `selfcheck.py` | 同一套状态断言 + 胶片带 11 张卡片不被裁切 | errors=0 |
| `sizes.py` / `sizes_tpl.py` | 9 / 8 种窗口尺寸下不溢出、卡片完整落在 dock / strip 内 | 全通过 |
| `probe_leftshadow.py` | 封面态「书左侧桌面有没有痕迹」8 档尺寸 × 双路取证（画布 alpha + 截图 RGB） | 8/8 |
| `probe_mid50.py` | 翻页停在 90° 那一帧，逐条带回读像素确认纸上有真实照片内容 | 通过 |

**第十轮合计 290 项检查全绿**（9 个套件；第九轮 286 → 本轮新增品牌改名迁移 4 项）。全部走页面内真实调用 + 真实鼠标事件（CDP `Input.dispatchMouseEvent`），不 mock。

#### 6.1 本轮修掉的一个"功能整段是死的"bug：书页图注

用户反馈：**「为什么这个文案功能没有用啊，选择了怎么显示出来没效果」**。

- 现象：在「选片成书」里点了一句文案，面板显示已写入，**书页上一个字都没有**。
- 根因：书页图注的抑制条件是 `plateHasText(im) = artMode()==='tpl' && !!im.title`。
  而成片对象的 `title` 来自 `opts.title`，**出厂值就是 `{name}`** ——
  于是**每张成片都带非空 title**，条件恒为真 → `capWillDraw` 恒为假 → 图注在任何页面上都不画。
  同时模版那行题字显示的是照片名，所以用户写的文案**在整本书里没有任何地方出现**。
- 正确语义：抑制是为了**避免重复**，所以要比**内容**而不是比**存在性** ——
  文案 ≠ 印片题字（照片名 vs 一句话）→ 画；文案 = 印片题字（`opts.title` 用 `{note}`，
  或图注来源选「照片名」）→ 不重复。
- 顺带补上第二条死路：**「满版出血」版式整段没有画图注的代码路径** ——
  写了文案又选满版，那句话同样一个字都不会出现。现在满版也画（底部柔光带 + 白字，位于页码上方）。
- 顺手加固：`two` / `sticker` 版式的图注原本没有自动缩字，用户手打长句会顶出版心；
  现在四个版式共用同一个 `fitCap()`（先缩字号、到下限再截断）。

**这一轮还暴露了两个"假通过"的测试**（都已修）：

1. `test_note` 的 N10 只"数图注带里的墨点"。而印片本身带墨、照片也在带里 ——
   一块照片就能凑够墨点数，于是"根本没画图注"也判成通过。
   现在改成**同一页"有文案 vs 无文案"的带内像素差**（实测 799 像素）。
2. `test_book` 的 B30 直接把**错误语义**固化成了断言（`plateHasText && !capWillDraw`，
   在错误实现下恰好为真）。现在改成"文案与印片题字不是同一句话时**必须真的画出来**"，
   并补了反向对照：把旧实现 patch 回产物另存一份，断言新守卫在那份上**必须失败**
   （实测旧实现下四个版式的图注带差异全为 0）。

- 11 套模版全部渲染成功，无异常
- 导出的 ZIP 经 Python `zipfile` 读取，CRC 校验通过、内容完整
- 控制台零报错

#### 6.2 图注字体：一个"只在用户机器上发生"的 bug

用户反馈：**「显示的字体太板正了，不太好看还跟右边显示的字体不一样」**（附截图）。

这一条和 6.1 的差别是：**6.1 我能在本机复现，6.2 不能。** 记录一下怎么把它钉死的。

- **先量，不猜。** 从用户截图里反推显示比例（8 个字占 101 px → 字号约 11.4 px
  → 880 画布对应约 0.50 显示比例），再用真实无头浏览器把窗口调到同一比例
  （`BV.pw≈456`，即 window 1850×1100）截图对照。同尺度下，用户那句的字形
  **没有一条细横**，是等笔宽的黑体；本机的真实截图是**横细竖粗的宋体**。
  还做了对照：把本机的宋体渲染过一遍 JPEG（模拟剪贴板压缩），细横**仍然在** ——
  所以"是不是压缩吃掉了细节"这个解释被排除。结论是硬的：
  **用户环境里这句话落到了黑体，本机落到宋体。**
- **根因是"静默降级"这个机制本身。** `Georgia,"Songti SC","SimSun",serif` 里
  没有任何一款字体能被浏览器保证找到；缺了它就用默认字体顶上，**且不报错**。
  于是同一份产物在不同环境看到两种字，而代码里没有任何地方记录这件事。
- **修法**：① 图注另起 `cap` 字族（首选楷体）；② 开机用位图探测逐个验证候选字族
  是否真的在（`fontOK`，见 §4.9），挑第一个确实装了的；③ 面板把结果写出来
  （"图注字体 楷体"）—— 这一行就是这一轮排查时最缺的信息。
  ④ 一个候选都验不出来时不退给 `serif` 去瞎猜，明确退到界面同一套黑体。
- **反向对照（这一轮的关键）**：把字族改回旧的 `serif` → 新断言 N30 必须挂；
  把一款**本机不存在**的中文字族写在前面（复刻用户环境）→ 中文落到黑体 → N28 必须挂。
  实测两种情形都如期失败（`.workbuddy/tmp/_neg_font.mjs`）。

**教训**：6.1 和 6.2 合起来是同一件事的两半 —— **"我看不到"不等于"不存在"，
也不等于"没关系"**。6.1 是本机就能复现，却被两个假绿测试蒙住；
6.2 是本机复现不了，只能靠"从用户截图反推尺度 + 同尺度对照 + 反向对照"钉住结论。
凡是依赖**环境**的东西（系统字族、浏览器、显示比例），都要留一条"把它写出来 / 量出来"的路径。

#### 6.3 悬停折角跑到书沟上（第九轮）

用户反馈：**「图书翻页有问题吧，怎么会在那个位置上出现呢」**（附截图）——
图中书页**中间缝**的位置翘起了一角，那里不该有折角。

- **现象定位**：折角是"悬停页面下缘那一侧时掀起一角"，按理只出现在书的**外侧两条边**上。
  用户图里它出现在**书沟**（跨页的中缝）上。
- **先确认"它到底被画在哪"**：一开始想用"找浅色三角"的办法量，量不到 ——
  那片是渐变，阈值挑不出边界。改成**同一帧「开折角 / 关折角」两张图的像素差**，
  差的 bbox 就是折角真正落在哪。这一步立刻给出硬数字：
  悬停左页 → 落点 `x∈[463,515]`，而书沟正好在 `463`；左页书口边在 `99`。**错得很干净。**
- **根因（一行坐标）**：`drawFold` 里锚点写成 `right ? this.spineX + pw : this.spineX`。
  `spineX` **在合书时是"当前页左边"、跨页时是"书沟"** —— 语义随模式漂移。
  左页折角要的是左页**书口边**（外侧）= `spineX - pw`，写成 `spineX` 就落到书沟上了。
  右页那半边本来就是 `+ pw`，所以一直是对的 → 症状是"只有左页会错"。
- **修法**：`right ? this.spineX + pw : this.spineX - pw`，两侧都显式取各自的书口边。
- **反向对照**：新增断言 ④（两侧折角都必须贴着书口边、且必须离开书沟）之外，
  再加 ④b —— 运行时把锚点**还回书沟**，同一判据必须判不合格
  （实测旧写法左折角落点 `x∈[468,516]`，书沟 469 → 如期挂）。
  没有 ④b 的话，④ 有可能只是"恰好通过"。

#### 6.4 改名：为什么"只改看得见的"，以及一处真实踩坑

第十轮把品牌从「光匣 LUMEN」改成「咔哒书 KADA」。做法上刻意划了条线：

- **改**：所有对用户可见的文字（网页标题、顶栏品牌、默认书名/署名/书脊、页眉页脚小字、
  导出文件名与压缩包说明、沉浸阅读标题、示例照片名、环衬上那枚印）。
- **不改**：存档 key `lumen.prefs.v1`、JS 命名空间 `window.LUMEN`（只加 `window.KADA` 别名）。
  这两个都不对用户显示，改掉一个会让老用户**丢存档**、另一个要把整套回归跟着改，
  收益是零。

**踩到的坑（真 bug，不是风格问题）**：第一版迁移我写成

```js
if(s.opts.title==='光匣')          s.opts.title=DEFAULT_OPTS.title;
if(s.opts.author==='LUMEN STUDIO') s.opts.author=DEFAULT_OPTS.author;
if(s.opts.spine==='LUMEN · 2026')  s.opts.spine=DEFAULT_OPTS.spine;
```

**`opts` 里根本没有 author / spine 这两个字段** —— 书名、署名、书脊是 `book` 的。
后果有两层：① `DEFAULT_OPTS.author` 是 `undefined`，迁移一跑就把字段**抹成 undefined**；
② `opts.title` 是**印片题字**，出厂值 `{name}`，从来不是「光匣」——
这条等于给"用户恰好把题字写成『光匣』"埋了个改字的雷。

是回归断言把它揪出来的：`{"t":"{name}", "bt":"咔哒书"}` ——
`author`/`spine` **从 JSON 里凭空消失了**（`undefined` 会被 `JSON.stringify` 丢掉），
`t` 又变成了 `{name}`。**字段"消失"是这类错误最典型的症状**：
不是值错了，是根本没写进去。

**顺带改成常量**：品牌名收成 `BRAND` / `OLD_BRAND` 两份，默认值与迁移共用 ——
"默认值改了、迁移里还写着旧名"这种脱节，只要两处各自写死就一定会再发生一次。

**教训**：改名这种"看起来纯机械"的活，风险全在**字段路径**上。
写迁移前先把结构读出来（`grep` 到定义那一行看清楚 `title/author/spine` 挂在谁下面），
不要凭"感觉应该在 opts 里"下手。

**教训**：这是"**变量语义随状态漂移**"型的 bug。`spineX` 不是"某条边的位置"，
而是"当前这条缝在哪"。凡是需要"**某一页的某条边**"的地方，都要显式写成 `spineX ± pw`，
不要依赖 `spineX` 此刻恰好是什么。另外，"折角位置"这类东西肉眼看是"感觉怪"，
但换成**开/关两张图的像素差**就变成一个可以被断言的坐标 —— 能被断言才能进回归。

---

## 七、可继续扩展的方向

> 第七轮已交付：氛围皮肤 7 套 · 一键氛围成书 · 文案库与心情标签 · 竖版导出与分享长图（详见 `咔哒书-优化升级方案.md` §9.1）。
> 下面这些仍未做。

1. **拖拽微调裁切**：目前缩放/偏移参数已就绪，接一个画布上的拖拽框即可
2. **每图独立文字**：模版已做到每张照片独立，但 `title / sub` 还是全局一套；
   把 `opts` 也下沉到照片级即可（数据结构与持久化方案都已就位）。
   注：`{note}` / `{mood}` / `{date}` 占位符已经让"每张不一样"这件事先落地了，剩下的是"每张完全不同的一句话"以外的字
3. **贴纸 / 手写涂鸦**（第二批）：需要新数据模型（贴纸是挂在照片坐标系上的对象，不是照片的属性），
   且要考虑缩放/旋转后的命中判定 —— 这是第二批的第一个硬骨头
4. **翻页动画导出视频**（第三批）：一本会自己翻页的相册，传播力最强，但成本与风险也最高
5. **书页拖拽排序**：选片条已支持拖动，改为拖拽重排顺序即可
6. **加深章节系统**：`titlecard` 模版已就绪，可按间隔自动插入并生成目录页
7. **真实 3D 书脊厚度**：给书盒加 `translateZ` 分层叠加，模拟纸张堆叠厚度
8. **导出 PDF**：将每页 canvas 用 `addImage` 写入 PDF，或走浏览器打印样式
9. **照片跨会话保存**：现在只存设置与分配；照片要真存下来得走 IndexedDB 存 Blob，
   并且要处理 file:// 下的配额与清理
10. **手机端**：本轮按拍板不做。已核实现状：固定宽度面板 + 桌面鼠标事件假设 + 每页一张 canvas
    的渲染预算，都决定了"直接上响应式"会得到能看不能用的一版，需要单独一轮
